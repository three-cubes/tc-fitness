"""CORE check: critical surfaces have executable, observed test evidence.

Static policy tests are useful for source-shape invariants, but they cannot
prove that a build, command, deployment adapter, or runtime works.  Consumers
declare critical surfaces and the real executable each surface depends on.
This hard gate then requires a marked test to invoke that executable, check
process success, and observe a produced artefact passed through the command.

The check is deliberately opt-in and repo-neutral.  An empty configuration is
a vacuous pass; once configured, every finding fails.
"""

from __future__ import annotations

import ast
import sys
from collections.abc import Mapping, Sequence
from dataclasses import dataclass
from pathlib import Path, PurePosixPath
from typing import Any, cast

from tc_fitness.check_evidence import report_finding
from tc_fitness.fitness_rule import FitnessRule
from tc_fitness.lib import remediation as _remediation

REMEDIATION = _remediation(
    fix=(
        "map every critical surface to a marked behavioural test that invokes the declared "
        "executable and asserts its result or a produced artefact"
    ),
    nxt="run the declared behavioural test, then re-run this check",
    run="python -m tc_fitness.core_checks.behavioural_evidence",
    passing="a real executable invocation followed by process and produced-output assertions",
    forbidden="source-text or configuration-shape assertions presented as behavioural evidence",
)

_SUBPROCESS_CALLS = frozenset(
    {
        "subprocess.run",
        "subprocess.Popen",
        "subprocess.call",
        "subprocess.check_call",
        "subprocess.check_output",
    }
)
_RESULT_ATTRIBUTES = frozenset({"returncode", "stdout", "stderr"})
_OUTPUT_OBSERVATIONS = frozenset(
    {"exists", "is_file", "is_dir", "read_text", "read_bytes", "stat", "iterdir"}
)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict


@dataclass(frozen=True, order=True)
class BehaviouralEvidenceFinding:
    """One deterministic evidence-integrity defect."""

    source: Path
    pointer: str
    code: str
    message: str
    fix: str
mutants_x__finding__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__finding__mutmut)
def _finding(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(Path(source), pointer, code, message, fix)


def x__finding__mutmut_orig(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(Path(source), pointer, code, message, fix)


def x__finding__mutmut_1(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(None, pointer, code, message, fix)


def x__finding__mutmut_2(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(Path(source), None, code, message, fix)


def x__finding__mutmut_3(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(Path(source), pointer, None, message, fix)


def x__finding__mutmut_4(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(Path(source), pointer, code, None, fix)


def x__finding__mutmut_5(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(Path(source), pointer, code, message, None)


def x__finding__mutmut_6(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(pointer, code, message, fix)


def x__finding__mutmut_7(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(Path(source), code, message, fix)


def x__finding__mutmut_8(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(Path(source), pointer, message, fix)


def x__finding__mutmut_9(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(Path(source), pointer, code, fix)


def x__finding__mutmut_10(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(Path(source), pointer, code, message, )


def x__finding__mutmut_11(
    source: str | Path, pointer: str, code: str, message: str, fix: str
) -> BehaviouralEvidenceFinding:
    return BehaviouralEvidenceFinding(Path(None), pointer, code, message, fix)

mutants_x__finding__mutmut['_mutmut_orig'] = x__finding__mutmut_orig # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_1'] = x__finding__mutmut_1 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_2'] = x__finding__mutmut_2 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_3'] = x__finding__mutmut_3 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_4'] = x__finding__mutmut_4 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_5'] = x__finding__mutmut_5 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_6'] = x__finding__mutmut_6 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_7'] = x__finding__mutmut_7 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_8'] = x__finding__mutmut_8 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_9'] = x__finding__mutmut_9 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_10'] = x__finding__mutmut_10 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_11'] = x__finding__mutmut_11 # type: ignore # mutmut generated
mutants_x__safe_relative_path__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__safe_relative_path__mutmut)
def _safe_relative_path(value: object) -> str | None:
    if not isinstance(value, str) or not value.strip():
        return None
    path = PurePosixPath(value)
    if path.is_absolute() or ".." in path.parts or "\\" in value:
        return None
    return path.as_posix()


def x__safe_relative_path__mutmut_orig(value: object) -> str | None:
    if not isinstance(value, str) or not value.strip():
        return None
    path = PurePosixPath(value)
    if path.is_absolute() or ".." in path.parts or "\\" in value:
        return None
    return path.as_posix()


def x__safe_relative_path__mutmut_1(value: object) -> str | None:
    if not isinstance(value, str) and not value.strip():
        return None
    path = PurePosixPath(value)
    if path.is_absolute() or ".." in path.parts or "\\" in value:
        return None
    return path.as_posix()


def x__safe_relative_path__mutmut_2(value: object) -> str | None:
    if isinstance(value, str) or not value.strip():
        return None
    path = PurePosixPath(value)
    if path.is_absolute() or ".." in path.parts or "\\" in value:
        return None
    return path.as_posix()


def x__safe_relative_path__mutmut_3(value: object) -> str | None:
    if not isinstance(value, str) or value.strip():
        return None
    path = PurePosixPath(value)
    if path.is_absolute() or ".." in path.parts or "\\" in value:
        return None
    return path.as_posix()


def x__safe_relative_path__mutmut_4(value: object) -> str | None:
    if not isinstance(value, str) or not value.strip():
        return None
    path = None
    if path.is_absolute() or ".." in path.parts or "\\" in value:
        return None
    return path.as_posix()


def x__safe_relative_path__mutmut_5(value: object) -> str | None:
    if not isinstance(value, str) or not value.strip():
        return None
    path = PurePosixPath(None)
    if path.is_absolute() or ".." in path.parts or "\\" in value:
        return None
    return path.as_posix()


def x__safe_relative_path__mutmut_6(value: object) -> str | None:
    if not isinstance(value, str) or not value.strip():
        return None
    path = PurePosixPath(value)
    if path.is_absolute() or ".." in path.parts and "\\" in value:
        return None
    return path.as_posix()


def x__safe_relative_path__mutmut_7(value: object) -> str | None:
    if not isinstance(value, str) or not value.strip():
        return None
    path = PurePosixPath(value)
    if path.is_absolute() and ".." in path.parts or "\\" in value:
        return None
    return path.as_posix()


def x__safe_relative_path__mutmut_8(value: object) -> str | None:
    if not isinstance(value, str) or not value.strip():
        return None
    path = PurePosixPath(value)
    if path.is_absolute() or "XX..XX" in path.parts or "\\" in value:
        return None
    return path.as_posix()


def x__safe_relative_path__mutmut_9(value: object) -> str | None:
    if not isinstance(value, str) or not value.strip():
        return None
    path = PurePosixPath(value)
    if path.is_absolute() or ".." not in path.parts or "\\" in value:
        return None
    return path.as_posix()


def x__safe_relative_path__mutmut_10(value: object) -> str | None:
    if not isinstance(value, str) or not value.strip():
        return None
    path = PurePosixPath(value)
    if path.is_absolute() or ".." in path.parts or "XX\\XX" in value:
        return None
    return path.as_posix()


def x__safe_relative_path__mutmut_11(value: object) -> str | None:
    if not isinstance(value, str) or not value.strip():
        return None
    path = PurePosixPath(value)
    if path.is_absolute() or ".." in path.parts or "\\" not in value:
        return None
    return path.as_posix()

mutants_x__safe_relative_path__mutmut['_mutmut_orig'] = x__safe_relative_path__mutmut_orig # type: ignore # mutmut generated
mutants_x__safe_relative_path__mutmut['x__safe_relative_path__mutmut_1'] = x__safe_relative_path__mutmut_1 # type: ignore # mutmut generated
mutants_x__safe_relative_path__mutmut['x__safe_relative_path__mutmut_2'] = x__safe_relative_path__mutmut_2 # type: ignore # mutmut generated
mutants_x__safe_relative_path__mutmut['x__safe_relative_path__mutmut_3'] = x__safe_relative_path__mutmut_3 # type: ignore # mutmut generated
mutants_x__safe_relative_path__mutmut['x__safe_relative_path__mutmut_4'] = x__safe_relative_path__mutmut_4 # type: ignore # mutmut generated
mutants_x__safe_relative_path__mutmut['x__safe_relative_path__mutmut_5'] = x__safe_relative_path__mutmut_5 # type: ignore # mutmut generated
mutants_x__safe_relative_path__mutmut['x__safe_relative_path__mutmut_6'] = x__safe_relative_path__mutmut_6 # type: ignore # mutmut generated
mutants_x__safe_relative_path__mutmut['x__safe_relative_path__mutmut_7'] = x__safe_relative_path__mutmut_7 # type: ignore # mutmut generated
mutants_x__safe_relative_path__mutmut['x__safe_relative_path__mutmut_8'] = x__safe_relative_path__mutmut_8 # type: ignore # mutmut generated
mutants_x__safe_relative_path__mutmut['x__safe_relative_path__mutmut_9'] = x__safe_relative_path__mutmut_9 # type: ignore # mutmut generated
mutants_x__safe_relative_path__mutmut['x__safe_relative_path__mutmut_10'] = x__safe_relative_path__mutmut_10 # type: ignore # mutmut generated
mutants_x__safe_relative_path__mutmut['x__safe_relative_path__mutmut_11'] = x__safe_relative_path__mutmut_11 # type: ignore # mutmut generated
mutants_x__sequence__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__sequence__mutmut)
def _sequence(value: object) -> tuple[object, ...] | None:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return None
    return tuple(value)


def x__sequence__mutmut_orig(value: object) -> tuple[object, ...] | None:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return None
    return tuple(value)


def x__sequence__mutmut_1(value: object) -> tuple[object, ...] | None:
    if not isinstance(value, Sequence) and isinstance(value, (str, bytes)):
        return None
    return tuple(value)


def x__sequence__mutmut_2(value: object) -> tuple[object, ...] | None:
    if isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return None
    return tuple(value)


def x__sequence__mutmut_3(value: object) -> tuple[object, ...] | None:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return None
    return tuple(None)

mutants_x__sequence__mutmut['_mutmut_orig'] = x__sequence__mutmut_orig # type: ignore # mutmut generated
mutants_x__sequence__mutmut['x__sequence__mutmut_1'] = x__sequence__mutmut_1 # type: ignore # mutmut generated
mutants_x__sequence__mutmut['x__sequence__mutmut_2'] = x__sequence__mutmut_2 # type: ignore # mutmut generated
mutants_x__sequence__mutmut['x__sequence__mutmut_3'] = x__sequence__mutmut_3 # type: ignore # mutmut generated
mutants_x__dotted_name__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__dotted_name__mutmut)
def _dotted_name(value: ast.expr) -> str | None:
    if isinstance(value, ast.Name):
        return value.id
    if isinstance(value, ast.Attribute):
        parent = _dotted_name(value.value)
        return f"{parent}.{value.attr}" if parent else None
    return None


def x__dotted_name__mutmut_orig(value: ast.expr) -> str | None:
    if isinstance(value, ast.Name):
        return value.id
    if isinstance(value, ast.Attribute):
        parent = _dotted_name(value.value)
        return f"{parent}.{value.attr}" if parent else None
    return None


def x__dotted_name__mutmut_1(value: ast.expr) -> str | None:
    if isinstance(value, ast.Name):
        return value.id
    if isinstance(value, ast.Attribute):
        parent = None
        return f"{parent}.{value.attr}" if parent else None
    return None


def x__dotted_name__mutmut_2(value: ast.expr) -> str | None:
    if isinstance(value, ast.Name):
        return value.id
    if isinstance(value, ast.Attribute):
        parent = _dotted_name(None)
        return f"{parent}.{value.attr}" if parent else None
    return None

mutants_x__dotted_name__mutmut['_mutmut_orig'] = x__dotted_name__mutmut_orig # type: ignore # mutmut generated
mutants_x__dotted_name__mutmut['x__dotted_name__mutmut_1'] = x__dotted_name__mutmut_1 # type: ignore # mutmut generated
mutants_x__dotted_name__mutmut['x__dotted_name__mutmut_2'] = x__dotted_name__mutmut_2 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__import_aliases__mutmut)
def _import_aliases(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_orig(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_1(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = None
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_2(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = None
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_3(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname and imported.name.split(".", maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_4(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(None, maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_5(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", maxsplit=None)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_6(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_7(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", )[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_8(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.rsplit(".", maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_9(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split("XX.XX", maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_10(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", maxsplit=2)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_11(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", maxsplit=1)[1]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_12(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", maxsplit=1)[0]
                aliases[binding] = None
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_13(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) or node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_14(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name == "*":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_15(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "XX*XX":
                    aliases[imported.asname or imported.name] = f"{node.module}.{imported.name}"
    return aliases


def x__import_aliases__mutmut_16(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname or imported.name] = None
    return aliases


def x__import_aliases__mutmut_17(tree: ast.Module) -> dict[str, str]:
    aliases: dict[str, str] = {}
    for node in tree.body:
        if isinstance(node, ast.Import):
            for imported in node.names:
                binding = imported.asname or imported.name.split(".", maxsplit=1)[0]
                aliases[binding] = imported.name if imported.asname else binding
        elif isinstance(node, ast.ImportFrom) and node.module:
            for imported in node.names:
                if imported.name != "*":
                    aliases[imported.asname and imported.name] = f"{node.module}.{imported.name}"
    return aliases

mutants_x__import_aliases__mutmut['_mutmut_orig'] = x__import_aliases__mutmut_orig # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_1'] = x__import_aliases__mutmut_1 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_2'] = x__import_aliases__mutmut_2 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_3'] = x__import_aliases__mutmut_3 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_4'] = x__import_aliases__mutmut_4 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_5'] = x__import_aliases__mutmut_5 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_6'] = x__import_aliases__mutmut_6 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_7'] = x__import_aliases__mutmut_7 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_8'] = x__import_aliases__mutmut_8 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_9'] = x__import_aliases__mutmut_9 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_10'] = x__import_aliases__mutmut_10 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_11'] = x__import_aliases__mutmut_11 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_12'] = x__import_aliases__mutmut_12 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_13'] = x__import_aliases__mutmut_13 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_14'] = x__import_aliases__mutmut_14 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_15'] = x__import_aliases__mutmut_15 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_16'] = x__import_aliases__mutmut_16 # type: ignore # mutmut generated
mutants_x__import_aliases__mutmut['x__import_aliases__mutmut_17'] = x__import_aliases__mutmut_17 # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__resolved_name__mutmut)
def _resolved_name(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(value)
    if dotted is None:
        return None
    root, *suffix = dotted.split(".")
    return ".".join((aliases.get(root, root), *suffix))


def x__resolved_name__mutmut_orig(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(value)
    if dotted is None:
        return None
    root, *suffix = dotted.split(".")
    return ".".join((aliases.get(root, root), *suffix))


def x__resolved_name__mutmut_1(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = None
    if dotted is None:
        return None
    root, *suffix = dotted.split(".")
    return ".".join((aliases.get(root, root), *suffix))


def x__resolved_name__mutmut_2(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(None)
    if dotted is None:
        return None
    root, *suffix = dotted.split(".")
    return ".".join((aliases.get(root, root), *suffix))


def x__resolved_name__mutmut_3(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(value)
    if dotted is not None:
        return None
    root, *suffix = dotted.split(".")
    return ".".join((aliases.get(root, root), *suffix))


def x__resolved_name__mutmut_4(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(value)
    if dotted is None:
        return None
    root, *suffix = None
    return ".".join((aliases.get(root, root), *suffix))


def x__resolved_name__mutmut_5(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(value)
    if dotted is None:
        return None
    root, *suffix = dotted.split(None)
    return ".".join((aliases.get(root, root), *suffix))


def x__resolved_name__mutmut_6(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(value)
    if dotted is None:
        return None
    root, *suffix = dotted.split("XX.XX")
    return ".".join((aliases.get(root, root), *suffix))


def x__resolved_name__mutmut_7(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(value)
    if dotted is None:
        return None
    root, *suffix = dotted.split(".")
    return ".".join(None)


def x__resolved_name__mutmut_8(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(value)
    if dotted is None:
        return None
    root, *suffix = dotted.split(".")
    return "XX.XX".join((aliases.get(root, root), *suffix))


def x__resolved_name__mutmut_9(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(value)
    if dotted is None:
        return None
    root, *suffix = dotted.split(".")
    return ".".join((aliases.get(None, root), *suffix))


def x__resolved_name__mutmut_10(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(value)
    if dotted is None:
        return None
    root, *suffix = dotted.split(".")
    return ".".join((aliases.get(root, None), *suffix))


def x__resolved_name__mutmut_11(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(value)
    if dotted is None:
        return None
    root, *suffix = dotted.split(".")
    return ".".join((aliases.get(root), *suffix))


def x__resolved_name__mutmut_12(value: ast.expr, aliases: Mapping[str, str]) -> str | None:
    dotted = _dotted_name(value)
    if dotted is None:
        return None
    root, *suffix = dotted.split(".")
    return ".".join((aliases.get(root, ), *suffix))

mutants_x__resolved_name__mutmut['_mutmut_orig'] = x__resolved_name__mutmut_orig # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut['x__resolved_name__mutmut_1'] = x__resolved_name__mutmut_1 # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut['x__resolved_name__mutmut_2'] = x__resolved_name__mutmut_2 # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut['x__resolved_name__mutmut_3'] = x__resolved_name__mutmut_3 # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut['x__resolved_name__mutmut_4'] = x__resolved_name__mutmut_4 # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut['x__resolved_name__mutmut_5'] = x__resolved_name__mutmut_5 # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut['x__resolved_name__mutmut_6'] = x__resolved_name__mutmut_6 # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut['x__resolved_name__mutmut_7'] = x__resolved_name__mutmut_7 # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut['x__resolved_name__mutmut_8'] = x__resolved_name__mutmut_8 # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut['x__resolved_name__mutmut_9'] = x__resolved_name__mutmut_9 # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut['x__resolved_name__mutmut_10'] = x__resolved_name__mutmut_10 # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut['x__resolved_name__mutmut_11'] = x__resolved_name__mutmut_11 # type: ignore # mutmut generated
mutants_x__resolved_name__mutmut['x__resolved_name__mutmut_12'] = x__resolved_name__mutmut_12 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__marker_names__mutmut)
def _marker_names(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_orig(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_1(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(None, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_2(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, None) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_3(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_4(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, ) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_5(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(None, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_6(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, None)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_7(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_8(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, )
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_9(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = None
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_10(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(None, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_11(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, None)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_12(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_13(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, )
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_14(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved or resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_15(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith(None):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_16(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("XXpytest.mark.XX"):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_17(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("PYTEST.MARK."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_18(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(None, maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_19(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=None)[0]}
    return set()


def x__marker_names__mutmut_20(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_21(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", )[0]}
    return set()


def x__marker_names__mutmut_22(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").rsplit(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_23(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix(None).split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_24(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removesuffix("pytest.mark.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_25(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("XXpytest.mark.XX").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_26(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("PYTEST.MARK.").split(".", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_27(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split("XX.XX", maxsplit=1)[0]}
    return set()


def x__marker_names__mutmut_28(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=2)[0]}
    return set()


def x__marker_names__mutmut_29(value: ast.expr, aliases: Mapping[str, str]) -> set[str]:
    if isinstance(value, ast.List | ast.Tuple):
        return set().union(*(_marker_names(item, aliases) for item in value.elts))
    if isinstance(value, ast.Call):
        return _marker_names(value.func, aliases)
    resolved = _resolved_name(value, aliases)
    if resolved and resolved.startswith("pytest.mark."):
        return {resolved.removeprefix("pytest.mark.").split(".", maxsplit=1)[1]}
    return set()

mutants_x__marker_names__mutmut['_mutmut_orig'] = x__marker_names__mutmut_orig # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_1'] = x__marker_names__mutmut_1 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_2'] = x__marker_names__mutmut_2 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_3'] = x__marker_names__mutmut_3 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_4'] = x__marker_names__mutmut_4 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_5'] = x__marker_names__mutmut_5 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_6'] = x__marker_names__mutmut_6 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_7'] = x__marker_names__mutmut_7 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_8'] = x__marker_names__mutmut_8 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_9'] = x__marker_names__mutmut_9 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_10'] = x__marker_names__mutmut_10 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_11'] = x__marker_names__mutmut_11 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_12'] = x__marker_names__mutmut_12 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_13'] = x__marker_names__mutmut_13 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_14'] = x__marker_names__mutmut_14 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_15'] = x__marker_names__mutmut_15 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_16'] = x__marker_names__mutmut_16 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_17'] = x__marker_names__mutmut_17 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_18'] = x__marker_names__mutmut_18 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_19'] = x__marker_names__mutmut_19 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_20'] = x__marker_names__mutmut_20 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_21'] = x__marker_names__mutmut_21 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_22'] = x__marker_names__mutmut_22 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_23'] = x__marker_names__mutmut_23 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_24'] = x__marker_names__mutmut_24 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_25'] = x__marker_names__mutmut_25 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_26'] = x__marker_names__mutmut_26 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_27'] = x__marker_names__mutmut_27 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_28'] = x__marker_names__mutmut_28 # type: ignore # mutmut generated
mutants_x__marker_names__mutmut['x__marker_names__mutmut_29'] = x__marker_names__mutmut_29 # type: ignore # mutmut generated
mutants_x__module_markers__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__module_markers__mutmut)
def _module_markers(tree: ast.Module, aliases: Mapping[str, str]) -> set[str]:
    for node in tree.body:
        if isinstance(node, ast.Assign) and any(
            isinstance(target, ast.Name) and target.id == "pytestmark" for target in node.targets
        ):
            return _marker_names(node.value, aliases)
    return set()


def x__module_markers__mutmut_orig(tree: ast.Module, aliases: Mapping[str, str]) -> set[str]:
    for node in tree.body:
        if isinstance(node, ast.Assign) and any(
            isinstance(target, ast.Name) and target.id == "pytestmark" for target in node.targets
        ):
            return _marker_names(node.value, aliases)
    return set()


def x__module_markers__mutmut_1(tree: ast.Module, aliases: Mapping[str, str]) -> set[str]:
    for node in tree.body:
        if isinstance(node, ast.Assign) or any(
            isinstance(target, ast.Name) and target.id == "pytestmark" for target in node.targets
        ):
            return _marker_names(node.value, aliases)
    return set()


def x__module_markers__mutmut_2(tree: ast.Module, aliases: Mapping[str, str]) -> set[str]:
    for node in tree.body:
        if isinstance(node, ast.Assign) and any(
            None
        ):
            return _marker_names(node.value, aliases)
    return set()


def x__module_markers__mutmut_3(tree: ast.Module, aliases: Mapping[str, str]) -> set[str]:
    for node in tree.body:
        if isinstance(node, ast.Assign) and any(
            isinstance(target, ast.Name) or target.id == "pytestmark" for target in node.targets
        ):
            return _marker_names(node.value, aliases)
    return set()


def x__module_markers__mutmut_4(tree: ast.Module, aliases: Mapping[str, str]) -> set[str]:
    for node in tree.body:
        if isinstance(node, ast.Assign) and any(
            isinstance(target, ast.Name) and target.id != "pytestmark" for target in node.targets
        ):
            return _marker_names(node.value, aliases)
    return set()


def x__module_markers__mutmut_5(tree: ast.Module, aliases: Mapping[str, str]) -> set[str]:
    for node in tree.body:
        if isinstance(node, ast.Assign) and any(
            isinstance(target, ast.Name) and target.id == "XXpytestmarkXX" for target in node.targets
        ):
            return _marker_names(node.value, aliases)
    return set()


def x__module_markers__mutmut_6(tree: ast.Module, aliases: Mapping[str, str]) -> set[str]:
    for node in tree.body:
        if isinstance(node, ast.Assign) and any(
            isinstance(target, ast.Name) and target.id == "PYTESTMARK" for target in node.targets
        ):
            return _marker_names(node.value, aliases)
    return set()


def x__module_markers__mutmut_7(tree: ast.Module, aliases: Mapping[str, str]) -> set[str]:
    for node in tree.body:
        if isinstance(node, ast.Assign) and any(
            isinstance(target, ast.Name) and target.id == "pytestmark" for target in node.targets
        ):
            return _marker_names(None, aliases)
    return set()


def x__module_markers__mutmut_8(tree: ast.Module, aliases: Mapping[str, str]) -> set[str]:
    for node in tree.body:
        if isinstance(node, ast.Assign) and any(
            isinstance(target, ast.Name) and target.id == "pytestmark" for target in node.targets
        ):
            return _marker_names(node.value, None)
    return set()


def x__module_markers__mutmut_9(tree: ast.Module, aliases: Mapping[str, str]) -> set[str]:
    for node in tree.body:
        if isinstance(node, ast.Assign) and any(
            isinstance(target, ast.Name) and target.id == "pytestmark" for target in node.targets
        ):
            return _marker_names(aliases)
    return set()


def x__module_markers__mutmut_10(tree: ast.Module, aliases: Mapping[str, str]) -> set[str]:
    for node in tree.body:
        if isinstance(node, ast.Assign) and any(
            isinstance(target, ast.Name) and target.id == "pytestmark" for target in node.targets
        ):
            return _marker_names(node.value, )
    return set()

mutants_x__module_markers__mutmut['_mutmut_orig'] = x__module_markers__mutmut_orig # type: ignore # mutmut generated
mutants_x__module_markers__mutmut['x__module_markers__mutmut_1'] = x__module_markers__mutmut_1 # type: ignore # mutmut generated
mutants_x__module_markers__mutmut['x__module_markers__mutmut_2'] = x__module_markers__mutmut_2 # type: ignore # mutmut generated
mutants_x__module_markers__mutmut['x__module_markers__mutmut_3'] = x__module_markers__mutmut_3 # type: ignore # mutmut generated
mutants_x__module_markers__mutmut['x__module_markers__mutmut_4'] = x__module_markers__mutmut_4 # type: ignore # mutmut generated
mutants_x__module_markers__mutmut['x__module_markers__mutmut_5'] = x__module_markers__mutmut_5 # type: ignore # mutmut generated
mutants_x__module_markers__mutmut['x__module_markers__mutmut_6'] = x__module_markers__mutmut_6 # type: ignore # mutmut generated
mutants_x__module_markers__mutmut['x__module_markers__mutmut_7'] = x__module_markers__mutmut_7 # type: ignore # mutmut generated
mutants_x__module_markers__mutmut['x__module_markers__mutmut_8'] = x__module_markers__mutmut_8 # type: ignore # mutmut generated
mutants_x__module_markers__mutmut['x__module_markers__mutmut_9'] = x__module_markers__mutmut_9 # type: ignore # mutmut generated
mutants_x__module_markers__mutmut['x__module_markers__mutmut_10'] = x__module_markers__mutmut_10 # type: ignore # mutmut generated
mutants_x__function_markers__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__function_markers__mutmut)
def _function_markers(node: ast.FunctionDef | ast.AsyncFunctionDef, aliases: Mapping[str, str]) -> set[str]:
    return set().union(*(_marker_names(decorator, aliases) for decorator in node.decorator_list))


def x__function_markers__mutmut_orig(node: ast.FunctionDef | ast.AsyncFunctionDef, aliases: Mapping[str, str]) -> set[str]:
    return set().union(*(_marker_names(decorator, aliases) for decorator in node.decorator_list))


def x__function_markers__mutmut_1(node: ast.FunctionDef | ast.AsyncFunctionDef, aliases: Mapping[str, str]) -> set[str]:
    return set().union(*(_marker_names(None, aliases) for decorator in node.decorator_list))


def x__function_markers__mutmut_2(node: ast.FunctionDef | ast.AsyncFunctionDef, aliases: Mapping[str, str]) -> set[str]:
    return set().union(*(_marker_names(decorator, None) for decorator in node.decorator_list))


def x__function_markers__mutmut_3(node: ast.FunctionDef | ast.AsyncFunctionDef, aliases: Mapping[str, str]) -> set[str]:
    return set().union(*(_marker_names(aliases) for decorator in node.decorator_list))


def x__function_markers__mutmut_4(node: ast.FunctionDef | ast.AsyncFunctionDef, aliases: Mapping[str, str]) -> set[str]:
    return set().union(*(_marker_names(decorator, ) for decorator in node.decorator_list))

mutants_x__function_markers__mutmut['_mutmut_orig'] = x__function_markers__mutmut_orig # type: ignore # mutmut generated
mutants_x__function_markers__mutmut['x__function_markers__mutmut_1'] = x__function_markers__mutmut_1 # type: ignore # mutmut generated
mutants_x__function_markers__mutmut['x__function_markers__mutmut_2'] = x__function_markers__mutmut_2 # type: ignore # mutmut generated
mutants_x__function_markers__mutmut['x__function_markers__mutmut_3'] = x__function_markers__mutmut_3 # type: ignore # mutmut generated
mutants_x__function_markers__mutmut['x__function_markers__mutmut_4'] = x__function_markers__mutmut_4 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__path_fragment__mutmut)
def _path_fragment(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_orig(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_1(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) or isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_2(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(None)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_3(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) or value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_4(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) or isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_5(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id not in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_6(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"XXstrXX", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_7(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"STR", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_8(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "XXPathXX"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_9(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_10(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "PATH"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_11(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(None, constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_12(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], None)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_13(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_14(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], )
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_15(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[1], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_16(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) or isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_17(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = None
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_18(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(None, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_19(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, None)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_20(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_21(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, )
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_22(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = None
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_23(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(None, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_24(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, None)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_25(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_26(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, )
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_27(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is not None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_28(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip(None)}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_29(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.lstrip('/')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_30(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('XX/XX')}/{right.lstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_31(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip(None)}" if left else right
    return None


def x__path_fragment__mutmut_32(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.rstrip('/')}" if left else right
    return None


def x__path_fragment__mutmut_33(value: ast.expr, constants: Mapping[str, str]) -> str | None:
    """Resolve literal/path-join syntax to a stable relative suffix."""
    if isinstance(value, ast.Constant) and isinstance(value.value, str):
        return value.value
    if isinstance(value, ast.Name):
        return constants.get(value.id)
    if isinstance(value, ast.Call) and isinstance(value.func, ast.Name) and value.func.id in {"str", "Path"}:
        if value.args:
            return _path_fragment(value.args[0], constants)
    if isinstance(value, ast.BinOp) and isinstance(value.op, ast.Div):
        left = _path_fragment(value.left, constants)
        right = _path_fragment(value.right, constants)
        if right is None:
            return None
        return f"{left.rstrip('/')}/{right.lstrip('XX/XX')}" if left else right
    return None

mutants_x__path_fragment__mutmut['_mutmut_orig'] = x__path_fragment__mutmut_orig # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_1'] = x__path_fragment__mutmut_1 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_2'] = x__path_fragment__mutmut_2 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_3'] = x__path_fragment__mutmut_3 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_4'] = x__path_fragment__mutmut_4 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_5'] = x__path_fragment__mutmut_5 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_6'] = x__path_fragment__mutmut_6 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_7'] = x__path_fragment__mutmut_7 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_8'] = x__path_fragment__mutmut_8 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_9'] = x__path_fragment__mutmut_9 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_10'] = x__path_fragment__mutmut_10 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_11'] = x__path_fragment__mutmut_11 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_12'] = x__path_fragment__mutmut_12 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_13'] = x__path_fragment__mutmut_13 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_14'] = x__path_fragment__mutmut_14 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_15'] = x__path_fragment__mutmut_15 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_16'] = x__path_fragment__mutmut_16 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_17'] = x__path_fragment__mutmut_17 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_18'] = x__path_fragment__mutmut_18 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_19'] = x__path_fragment__mutmut_19 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_20'] = x__path_fragment__mutmut_20 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_21'] = x__path_fragment__mutmut_21 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_22'] = x__path_fragment__mutmut_22 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_23'] = x__path_fragment__mutmut_23 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_24'] = x__path_fragment__mutmut_24 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_25'] = x__path_fragment__mutmut_25 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_26'] = x__path_fragment__mutmut_26 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_27'] = x__path_fragment__mutmut_27 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_28'] = x__path_fragment__mutmut_28 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_29'] = x__path_fragment__mutmut_29 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_30'] = x__path_fragment__mutmut_30 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_31'] = x__path_fragment__mutmut_31 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_32'] = x__path_fragment__mutmut_32 # type: ignore # mutmut generated
mutants_x__path_fragment__mutmut['x__path_fragment__mutmut_33'] = x__path_fragment__mutmut_33 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__path_constants__mutmut)
def _path_constants(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_orig(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_1(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = None
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_2(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = None
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_3(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(None):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_4(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) - 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_5(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 2):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_6(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = None
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_7(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = True
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_8(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = None
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_9(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(None, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_10(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, None)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_11(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_12(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, )
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_13(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is not None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_14(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                break
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_15(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) or constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_16(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(None) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_17(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) == fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_18(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = None
                    changed = True
        if not changed:
            break
    return constants


def x__path_constants__mutmut_19(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = None
        if not changed:
            break
    return constants


def x__path_constants__mutmut_20(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = False
        if not changed:
            break
    return constants


def x__path_constants__mutmut_21(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if changed:
            break
    return constants


def x__path_constants__mutmut_22(tree: ast.Module) -> dict[str, str]:
    constants: dict[str, str] = {}
    assignments = [node for node in tree.body if isinstance(node, ast.Assign)]
    for _ in range(len(assignments) + 1):
        changed = False
        for node in assignments:
            fragment = _path_fragment(node.value, constants)
            if fragment is None:
                continue
            for target in node.targets:
                if isinstance(target, ast.Name) and constants.get(target.id) != fragment:
                    constants[target.id] = fragment
                    changed = True
        if not changed:
            return
    return constants

mutants_x__path_constants__mutmut['_mutmut_orig'] = x__path_constants__mutmut_orig # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_1'] = x__path_constants__mutmut_1 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_2'] = x__path_constants__mutmut_2 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_3'] = x__path_constants__mutmut_3 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_4'] = x__path_constants__mutmut_4 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_5'] = x__path_constants__mutmut_5 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_6'] = x__path_constants__mutmut_6 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_7'] = x__path_constants__mutmut_7 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_8'] = x__path_constants__mutmut_8 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_9'] = x__path_constants__mutmut_9 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_10'] = x__path_constants__mutmut_10 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_11'] = x__path_constants__mutmut_11 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_12'] = x__path_constants__mutmut_12 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_13'] = x__path_constants__mutmut_13 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_14'] = x__path_constants__mutmut_14 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_15'] = x__path_constants__mutmut_15 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_16'] = x__path_constants__mutmut_16 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_17'] = x__path_constants__mutmut_17 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_18'] = x__path_constants__mutmut_18 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_19'] = x__path_constants__mutmut_19 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_20'] = x__path_constants__mutmut_20 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_21'] = x__path_constants__mutmut_21 # type: ignore # mutmut generated
mutants_x__path_constants__mutmut['x__path_constants__mutmut_22'] = x__path_constants__mutmut_22 # type: ignore # mutmut generated
mutants_x__command_expression__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__command_expression__mutmut)
def _command_expression(call: ast.Call) -> ast.expr | None:
    if not call.args:
        return None
    argv = call.args[0]
    if isinstance(argv, ast.List | ast.Tuple) and argv.elts:
        return argv.elts[0]
    return argv


def x__command_expression__mutmut_orig(call: ast.Call) -> ast.expr | None:
    if not call.args:
        return None
    argv = call.args[0]
    if isinstance(argv, ast.List | ast.Tuple) and argv.elts:
        return argv.elts[0]
    return argv


def x__command_expression__mutmut_1(call: ast.Call) -> ast.expr | None:
    if call.args:
        return None
    argv = call.args[0]
    if isinstance(argv, ast.List | ast.Tuple) and argv.elts:
        return argv.elts[0]
    return argv


def x__command_expression__mutmut_2(call: ast.Call) -> ast.expr | None:
    if not call.args:
        return None
    argv = None
    if isinstance(argv, ast.List | ast.Tuple) and argv.elts:
        return argv.elts[0]
    return argv


def x__command_expression__mutmut_3(call: ast.Call) -> ast.expr | None:
    if not call.args:
        return None
    argv = call.args[1]
    if isinstance(argv, ast.List | ast.Tuple) and argv.elts:
        return argv.elts[0]
    return argv


def x__command_expression__mutmut_4(call: ast.Call) -> ast.expr | None:
    if not call.args:
        return None
    argv = call.args[0]
    if isinstance(argv, ast.List | ast.Tuple) or argv.elts:
        return argv.elts[0]
    return argv


def x__command_expression__mutmut_5(call: ast.Call) -> ast.expr | None:
    if not call.args:
        return None
    argv = call.args[0]
    if isinstance(argv, ast.List | ast.Tuple) and argv.elts:
        return argv.elts[1]
    return argv

mutants_x__command_expression__mutmut['_mutmut_orig'] = x__command_expression__mutmut_orig # type: ignore # mutmut generated
mutants_x__command_expression__mutmut['x__command_expression__mutmut_1'] = x__command_expression__mutmut_1 # type: ignore # mutmut generated
mutants_x__command_expression__mutmut['x__command_expression__mutmut_2'] = x__command_expression__mutmut_2 # type: ignore # mutmut generated
mutants_x__command_expression__mutmut['x__command_expression__mutmut_3'] = x__command_expression__mutmut_3 # type: ignore # mutmut generated
mutants_x__command_expression__mutmut['x__command_expression__mutmut_4'] = x__command_expression__mutmut_4 # type: ignore # mutmut generated
mutants_x__command_expression__mutmut['x__command_expression__mutmut_5'] = x__command_expression__mutmut_5 # type: ignore # mutmut generated
mutants_x__normalise_suffix__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__normalise_suffix__mutmut)
def _normalise_suffix(value: str) -> str:
    return PurePosixPath(value).as_posix().lstrip("./")


def x__normalise_suffix__mutmut_orig(value: str) -> str:
    return PurePosixPath(value).as_posix().lstrip("./")


def x__normalise_suffix__mutmut_1(value: str) -> str:
    return PurePosixPath(value).as_posix().lstrip(None)


def x__normalise_suffix__mutmut_2(value: str) -> str:
    return PurePosixPath(value).as_posix().rstrip("./")


def x__normalise_suffix__mutmut_3(value: str) -> str:
    return PurePosixPath(None).as_posix().lstrip("./")


def x__normalise_suffix__mutmut_4(value: str) -> str:
    return PurePosixPath(value).as_posix().lstrip("XX./XX")

mutants_x__normalise_suffix__mutmut['_mutmut_orig'] = x__normalise_suffix__mutmut_orig # type: ignore # mutmut generated
mutants_x__normalise_suffix__mutmut['x__normalise_suffix__mutmut_1'] = x__normalise_suffix__mutmut_1 # type: ignore # mutmut generated
mutants_x__normalise_suffix__mutmut['x__normalise_suffix__mutmut_2'] = x__normalise_suffix__mutmut_2 # type: ignore # mutmut generated
mutants_x__normalise_suffix__mutmut['x__normalise_suffix__mutmut_3'] = x__normalise_suffix__mutmut_3 # type: ignore # mutmut generated
mutants_x__normalise_suffix__mutmut['x__normalise_suffix__mutmut_4'] = x__normalise_suffix__mutmut_4 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__call_executes__mutmut)
def _call_executes(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_orig(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_1(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(None, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_2(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, None) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_3(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_4(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, ) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_5(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_6(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return True
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_7(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = None
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_8(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(None)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_9(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_10(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(None, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_11(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, None) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_12(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_13(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, ) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_14(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_15(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is not None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_16(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return True
    return _normalise_suffix(fragment).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_17(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(None)


def x__call_executes__mutmut_18(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(None).endswith(_normalise_suffix(executable))


def x__call_executes__mutmut_19(
    call: ast.Call,
    *,
    executable: str,
    aliases: Mapping[str, str],
    constants: Mapping[str, str],
) -> bool:
    if _resolved_name(call.func, aliases) not in _SUBPROCESS_CALLS:
        return False
    command = _command_expression(call)
    fragment = _path_fragment(command, constants) if command is not None else None
    if fragment is None:
        return False
    return _normalise_suffix(fragment).endswith(_normalise_suffix(None))

mutants_x__call_executes__mutmut['_mutmut_orig'] = x__call_executes__mutmut_orig # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_1'] = x__call_executes__mutmut_1 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_2'] = x__call_executes__mutmut_2 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_3'] = x__call_executes__mutmut_3 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_4'] = x__call_executes__mutmut_4 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_5'] = x__call_executes__mutmut_5 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_6'] = x__call_executes__mutmut_6 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_7'] = x__call_executes__mutmut_7 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_8'] = x__call_executes__mutmut_8 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_9'] = x__call_executes__mutmut_9 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_10'] = x__call_executes__mutmut_10 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_11'] = x__call_executes__mutmut_11 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_12'] = x__call_executes__mutmut_12 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_13'] = x__call_executes__mutmut_13 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_14'] = x__call_executes__mutmut_14 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_15'] = x__call_executes__mutmut_15 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_16'] = x__call_executes__mutmut_16 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_17'] = x__call_executes__mutmut_17 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_18'] = x__call_executes__mutmut_18 # type: ignore # mutmut generated
mutants_x__call_executes__mutmut['x__call_executes__mutmut_19'] = x__call_executes__mutmut_19 # type: ignore # mutmut generated
mutants_x__names_and_attributes__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__names_and_attributes__mutmut)
def _names_and_attributes(node: ast.AST) -> tuple[set[str], set[tuple[str, str]]]:
    names: set[str] = set()
    attributes: set[tuple[str, str]] = set()
    for child in ast.walk(node):
        if isinstance(child, ast.Name):
            names.add(child.id)
        elif isinstance(child, ast.Attribute) and isinstance(child.value, ast.Name):
            attributes.add((child.value.id, child.attr))
    return names, attributes


def x__names_and_attributes__mutmut_orig(node: ast.AST) -> tuple[set[str], set[tuple[str, str]]]:
    names: set[str] = set()
    attributes: set[tuple[str, str]] = set()
    for child in ast.walk(node):
        if isinstance(child, ast.Name):
            names.add(child.id)
        elif isinstance(child, ast.Attribute) and isinstance(child.value, ast.Name):
            attributes.add((child.value.id, child.attr))
    return names, attributes


def x__names_and_attributes__mutmut_1(node: ast.AST) -> tuple[set[str], set[tuple[str, str]]]:
    names: set[str] = None
    attributes: set[tuple[str, str]] = set()
    for child in ast.walk(node):
        if isinstance(child, ast.Name):
            names.add(child.id)
        elif isinstance(child, ast.Attribute) and isinstance(child.value, ast.Name):
            attributes.add((child.value.id, child.attr))
    return names, attributes


def x__names_and_attributes__mutmut_2(node: ast.AST) -> tuple[set[str], set[tuple[str, str]]]:
    names: set[str] = set()
    attributes: set[tuple[str, str]] = None
    for child in ast.walk(node):
        if isinstance(child, ast.Name):
            names.add(child.id)
        elif isinstance(child, ast.Attribute) and isinstance(child.value, ast.Name):
            attributes.add((child.value.id, child.attr))
    return names, attributes


def x__names_and_attributes__mutmut_3(node: ast.AST) -> tuple[set[str], set[tuple[str, str]]]:
    names: set[str] = set()
    attributes: set[tuple[str, str]] = set()
    for child in ast.walk(None):
        if isinstance(child, ast.Name):
            names.add(child.id)
        elif isinstance(child, ast.Attribute) and isinstance(child.value, ast.Name):
            attributes.add((child.value.id, child.attr))
    return names, attributes


def x__names_and_attributes__mutmut_4(node: ast.AST) -> tuple[set[str], set[tuple[str, str]]]:
    names: set[str] = set()
    attributes: set[tuple[str, str]] = set()
    for child in ast.walk(node):
        if isinstance(child, ast.Name):
            names.add(None)
        elif isinstance(child, ast.Attribute) and isinstance(child.value, ast.Name):
            attributes.add((child.value.id, child.attr))
    return names, attributes


def x__names_and_attributes__mutmut_5(node: ast.AST) -> tuple[set[str], set[tuple[str, str]]]:
    names: set[str] = set()
    attributes: set[tuple[str, str]] = set()
    for child in ast.walk(node):
        if isinstance(child, ast.Name):
            names.add(child.id)
        elif isinstance(child, ast.Attribute) or isinstance(child.value, ast.Name):
            attributes.add((child.value.id, child.attr))
    return names, attributes


def x__names_and_attributes__mutmut_6(node: ast.AST) -> tuple[set[str], set[tuple[str, str]]]:
    names: set[str] = set()
    attributes: set[tuple[str, str]] = set()
    for child in ast.walk(node):
        if isinstance(child, ast.Name):
            names.add(child.id)
        elif isinstance(child, ast.Attribute) and isinstance(child.value, ast.Name):
            attributes.add(None)
    return names, attributes

mutants_x__names_and_attributes__mutmut['_mutmut_orig'] = x__names_and_attributes__mutmut_orig # type: ignore # mutmut generated
mutants_x__names_and_attributes__mutmut['x__names_and_attributes__mutmut_1'] = x__names_and_attributes__mutmut_1 # type: ignore # mutmut generated
mutants_x__names_and_attributes__mutmut['x__names_and_attributes__mutmut_2'] = x__names_and_attributes__mutmut_2 # type: ignore # mutmut generated
mutants_x__names_and_attributes__mutmut['x__names_and_attributes__mutmut_3'] = x__names_and_attributes__mutmut_3 # type: ignore # mutmut generated
mutants_x__names_and_attributes__mutmut['x__names_and_attributes__mutmut_4'] = x__names_and_attributes__mutmut_4 # type: ignore # mutmut generated
mutants_x__names_and_attributes__mutmut['x__names_and_attributes__mutmut_5'] = x__names_and_attributes__mutmut_5 # type: ignore # mutmut generated
mutants_x__names_and_attributes__mutmut['x__names_and_attributes__mutmut_6'] = x__names_and_attributes__mutmut_6 # type: ignore # mutmut generated
mutants_x__assert_observes_process__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__assert_observes_process__mutmut)
def _assert_observes_process(assertion: ast.Assert, result_names: set[str]) -> bool:
    _, attributes = _names_and_attributes(assertion.test)
    return any(name in result_names and attribute in _RESULT_ATTRIBUTES for name, attribute in attributes)


def x__assert_observes_process__mutmut_orig(assertion: ast.Assert, result_names: set[str]) -> bool:
    _, attributes = _names_and_attributes(assertion.test)
    return any(name in result_names and attribute in _RESULT_ATTRIBUTES for name, attribute in attributes)


def x__assert_observes_process__mutmut_1(assertion: ast.Assert, result_names: set[str]) -> bool:
    _, attributes = None
    return any(name in result_names and attribute in _RESULT_ATTRIBUTES for name, attribute in attributes)


def x__assert_observes_process__mutmut_2(assertion: ast.Assert, result_names: set[str]) -> bool:
    _, attributes = _names_and_attributes(None)
    return any(name in result_names and attribute in _RESULT_ATTRIBUTES for name, attribute in attributes)


def x__assert_observes_process__mutmut_3(assertion: ast.Assert, result_names: set[str]) -> bool:
    _, attributes = _names_and_attributes(assertion.test)
    return any(None)


def x__assert_observes_process__mutmut_4(assertion: ast.Assert, result_names: set[str]) -> bool:
    _, attributes = _names_and_attributes(assertion.test)
    return any(name in result_names or attribute in _RESULT_ATTRIBUTES for name, attribute in attributes)


def x__assert_observes_process__mutmut_5(assertion: ast.Assert, result_names: set[str]) -> bool:
    _, attributes = _names_and_attributes(assertion.test)
    return any(name not in result_names and attribute in _RESULT_ATTRIBUTES for name, attribute in attributes)


def x__assert_observes_process__mutmut_6(assertion: ast.Assert, result_names: set[str]) -> bool:
    _, attributes = _names_and_attributes(assertion.test)
    return any(name in result_names and attribute not in _RESULT_ATTRIBUTES for name, attribute in attributes)

mutants_x__assert_observes_process__mutmut['_mutmut_orig'] = x__assert_observes_process__mutmut_orig # type: ignore # mutmut generated
mutants_x__assert_observes_process__mutmut['x__assert_observes_process__mutmut_1'] = x__assert_observes_process__mutmut_1 # type: ignore # mutmut generated
mutants_x__assert_observes_process__mutmut['x__assert_observes_process__mutmut_2'] = x__assert_observes_process__mutmut_2 # type: ignore # mutmut generated
mutants_x__assert_observes_process__mutmut['x__assert_observes_process__mutmut_3'] = x__assert_observes_process__mutmut_3 # type: ignore # mutmut generated
mutants_x__assert_observes_process__mutmut['x__assert_observes_process__mutmut_4'] = x__assert_observes_process__mutmut_4 # type: ignore # mutmut generated
mutants_x__assert_observes_process__mutmut['x__assert_observes_process__mutmut_5'] = x__assert_observes_process__mutmut_5 # type: ignore # mutmut generated
mutants_x__assert_observes_process__mutmut['x__assert_observes_process__mutmut_6'] = x__assert_observes_process__mutmut_6 # type: ignore # mutmut generated
mutants_x__assert_observes_output__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__assert_observes_output__mutmut)
def _assert_observes_output(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(assertion.test):
        if not (
            isinstance(child, ast.Call)
            and isinstance(child.func, ast.Attribute)
            and child.func.attr in _OUTPUT_OBSERVATIONS
        ):
            continue
        names, _ = _names_and_attributes(child.func.value)
        if names & command_names:
            return True
    return False


def x__assert_observes_output__mutmut_orig(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(assertion.test):
        if not (
            isinstance(child, ast.Call)
            and isinstance(child.func, ast.Attribute)
            and child.func.attr in _OUTPUT_OBSERVATIONS
        ):
            continue
        names, _ = _names_and_attributes(child.func.value)
        if names & command_names:
            return True
    return False


def x__assert_observes_output__mutmut_1(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(None):
        if not (
            isinstance(child, ast.Call)
            and isinstance(child.func, ast.Attribute)
            and child.func.attr in _OUTPUT_OBSERVATIONS
        ):
            continue
        names, _ = _names_and_attributes(child.func.value)
        if names & command_names:
            return True
    return False


def x__assert_observes_output__mutmut_2(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(assertion.test):
        if (
            isinstance(child, ast.Call)
            and isinstance(child.func, ast.Attribute)
            and child.func.attr in _OUTPUT_OBSERVATIONS
        ):
            continue
        names, _ = _names_and_attributes(child.func.value)
        if names & command_names:
            return True
    return False


def x__assert_observes_output__mutmut_3(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(assertion.test):
        if not (
            isinstance(child, ast.Call)
            and isinstance(child.func, ast.Attribute) or child.func.attr in _OUTPUT_OBSERVATIONS
        ):
            continue
        names, _ = _names_and_attributes(child.func.value)
        if names & command_names:
            return True
    return False


def x__assert_observes_output__mutmut_4(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(assertion.test):
        if not (
            isinstance(child, ast.Call) or isinstance(child.func, ast.Attribute)
            and child.func.attr in _OUTPUT_OBSERVATIONS
        ):
            continue
        names, _ = _names_and_attributes(child.func.value)
        if names & command_names:
            return True
    return False


def x__assert_observes_output__mutmut_5(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(assertion.test):
        if not (
            isinstance(child, ast.Call)
            and isinstance(child.func, ast.Attribute)
            and child.func.attr not in _OUTPUT_OBSERVATIONS
        ):
            continue
        names, _ = _names_and_attributes(child.func.value)
        if names & command_names:
            return True
    return False


def x__assert_observes_output__mutmut_6(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(assertion.test):
        if not (
            isinstance(child, ast.Call)
            and isinstance(child.func, ast.Attribute)
            and child.func.attr in _OUTPUT_OBSERVATIONS
        ):
            break
        names, _ = _names_and_attributes(child.func.value)
        if names & command_names:
            return True
    return False


def x__assert_observes_output__mutmut_7(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(assertion.test):
        if not (
            isinstance(child, ast.Call)
            and isinstance(child.func, ast.Attribute)
            and child.func.attr in _OUTPUT_OBSERVATIONS
        ):
            continue
        names, _ = None
        if names & command_names:
            return True
    return False


def x__assert_observes_output__mutmut_8(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(assertion.test):
        if not (
            isinstance(child, ast.Call)
            and isinstance(child.func, ast.Attribute)
            and child.func.attr in _OUTPUT_OBSERVATIONS
        ):
            continue
        names, _ = _names_and_attributes(None)
        if names & command_names:
            return True
    return False


def x__assert_observes_output__mutmut_9(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(assertion.test):
        if not (
            isinstance(child, ast.Call)
            and isinstance(child.func, ast.Attribute)
            and child.func.attr in _OUTPUT_OBSERVATIONS
        ):
            continue
        names, _ = _names_and_attributes(child.func.value)
        if names | command_names:
            return True
    return False


def x__assert_observes_output__mutmut_10(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(assertion.test):
        if not (
            isinstance(child, ast.Call)
            and isinstance(child.func, ast.Attribute)
            and child.func.attr in _OUTPUT_OBSERVATIONS
        ):
            continue
        names, _ = _names_and_attributes(child.func.value)
        if names & command_names:
            return False
    return False


def x__assert_observes_output__mutmut_11(assertion: ast.Assert, command_names: set[str]) -> bool:
    for child in ast.walk(assertion.test):
        if not (
            isinstance(child, ast.Call)
            and isinstance(child.func, ast.Attribute)
            and child.func.attr in _OUTPUT_OBSERVATIONS
        ):
            continue
        names, _ = _names_and_attributes(child.func.value)
        if names & command_names:
            return True
    return True

mutants_x__assert_observes_output__mutmut['_mutmut_orig'] = x__assert_observes_output__mutmut_orig # type: ignore # mutmut generated
mutants_x__assert_observes_output__mutmut['x__assert_observes_output__mutmut_1'] = x__assert_observes_output__mutmut_1 # type: ignore # mutmut generated
mutants_x__assert_observes_output__mutmut['x__assert_observes_output__mutmut_2'] = x__assert_observes_output__mutmut_2 # type: ignore # mutmut generated
mutants_x__assert_observes_output__mutmut['x__assert_observes_output__mutmut_3'] = x__assert_observes_output__mutmut_3 # type: ignore # mutmut generated
mutants_x__assert_observes_output__mutmut['x__assert_observes_output__mutmut_4'] = x__assert_observes_output__mutmut_4 # type: ignore # mutmut generated
mutants_x__assert_observes_output__mutmut['x__assert_observes_output__mutmut_5'] = x__assert_observes_output__mutmut_5 # type: ignore # mutmut generated
mutants_x__assert_observes_output__mutmut['x__assert_observes_output__mutmut_6'] = x__assert_observes_output__mutmut_6 # type: ignore # mutmut generated
mutants_x__assert_observes_output__mutmut['x__assert_observes_output__mutmut_7'] = x__assert_observes_output__mutmut_7 # type: ignore # mutmut generated
mutants_x__assert_observes_output__mutmut['x__assert_observes_output__mutmut_8'] = x__assert_observes_output__mutmut_8 # type: ignore # mutmut generated
mutants_x__assert_observes_output__mutmut['x__assert_observes_output__mutmut_9'] = x__assert_observes_output__mutmut_9 # type: ignore # mutmut generated
mutants_x__assert_observes_output__mutmut['x__assert_observes_output__mutmut_10'] = x__assert_observes_output__mutmut_10 # type: ignore # mutmut generated
mutants_x__assert_observes_output__mutmut['x__assert_observes_output__mutmut_11'] = x__assert_observes_output__mutmut_11 # type: ignore # mutmut generated


@dataclass(frozen=True)
class _TestEvidence:
    has_marker: bool
    executes: bool
    observes_process: bool
    observes_output: bool
mutants_x__test_file_evidence__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__test_file_evidence__mutmut)
def _test_file_evidence(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_orig(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_1(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = None
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_2(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(None, filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_3(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=None)
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_4(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_5(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), )
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_6(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding=None), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_7(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="XXutf-8XX"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_8(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="UTF-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_9(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(None))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_10(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(None, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_11(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, None, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_12(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, None, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_13(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, None)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_14(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_15(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_16(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_17(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, )
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_18(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(True, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_19(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, True, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_20(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, True, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_21(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, True)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_22(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = None
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_23(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(None)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_24(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = None
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_25(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(None, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_26(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, None)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_27(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_28(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, )
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_29(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = None
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_30(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(None)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_31(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = None
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_32(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = True
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_33(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = None
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_34(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = True
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_35(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = None
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_36(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = True
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_37(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = None
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_38(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = True
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_39(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(None):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_40(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) and not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_41(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_42(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_43(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith(None):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_44(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("XXtest_XX"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_45(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("TEST_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_46(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            break
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_47(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_48(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) | markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_49(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers & _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_50(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(None, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_51(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, None)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_52(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_53(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, )) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_54(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            break
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_55(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = None
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_56(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = False
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_57(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = None
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_58(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = None
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_59(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = None
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_60(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = True
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_61(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = None
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_62(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(None):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_63(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = ""
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_64(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = ""
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_65(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) or isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_66(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = None
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_67(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 or isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_68(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) != 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_69(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 2 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_70(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = None
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_71(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[1].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_72(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) or isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_73(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = None
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_74(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = None
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_75(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) or isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_76(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = None
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_77(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None and not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_78(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is not None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_79(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_80(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                None, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_81(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=None, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_82(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=None, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_83(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=None
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_84(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_85(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_86(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_87(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_88(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                break
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_89(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = None
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_90(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = False
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_91(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(None)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_92(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(None)
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_93(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(None) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_94(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = None
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_95(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced and any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_96(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                None
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_97(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant) or keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_98(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check" or isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_99(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg != "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_100(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "XXcheckXX"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_101(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "CHECK"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_102(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is not True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_103(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is False
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_104(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(None)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_105(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_106(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            break
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_107(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = None
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_108(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(None)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_109(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced and any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_110(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            None
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_111(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution or _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_112(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert) or node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_113(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno >= first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_114(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(None, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_115(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, None)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_116(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_117(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, )
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_118(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(None)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_119(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = None
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_120(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = False
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_121(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            None
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_122(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution or _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_123(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert) or node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_124(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno >= first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_125(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(None, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_126(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, None)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_127(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_128(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, )
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_129(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(None)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_130(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = None
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_131(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = False
    return _TestEvidence(has_marker, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_132(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(None, executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_133(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, None, observes_process, observes_output)


def x__test_file_evidence__mutmut_134(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, None, observes_output)


def x__test_file_evidence__mutmut_135(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, None)


def x__test_file_evidence__mutmut_136(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(executes, observes_process, observes_output)


def x__test_file_evidence__mutmut_137(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, observes_process, observes_output)


def x__test_file_evidence__mutmut_138(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_output)


def x__test_file_evidence__mutmut_139(path: Path, *, executable: str, markers: frozenset[str]) -> _TestEvidence:
    try:
        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
    except (OSError, SyntaxError, UnicodeDecodeError):
        return _TestEvidence(False, False, False, False)
    aliases = _import_aliases(tree)
    module_markers = _module_markers(tree, aliases)
    constants = _path_constants(tree)
    has_marker = False
    executes = False
    observes_process = False
    observes_output = False
    for test in ast.walk(tree):
        if not isinstance(test, ast.FunctionDef | ast.AsyncFunctionDef) or not test.name.startswith("test_"):
            continue
        if not ((module_markers | _function_markers(test, aliases)) & markers):
            continue
        has_marker = True
        result_names: set[str] = set()
        command_names: set[str] = set()
        check_enforced = False
        execution_lines: list[int] = []
        for node in ast.walk(test):
            call: ast.Call | None = None
            result_name: str | None = None
            if isinstance(node, ast.Assign) and isinstance(node.value, ast.Call):
                call = node.value
                if len(node.targets) == 1 and isinstance(node.targets[0], ast.Name):
                    result_name = node.targets[0].id
            elif isinstance(node, ast.AnnAssign) and isinstance(node.value, ast.Call):
                call = node.value
                if isinstance(node.target, ast.Name):
                    result_name = node.target.id
            elif isinstance(node, ast.Expr) and isinstance(node.value, ast.Call):
                call = node.value
            if call is None or not _call_executes(
                call, executable=executable, aliases=aliases, constants=constants
            ):
                continue
            executes = True
            execution_lines.append(call.lineno)
            command_names.update(child.id for child in ast.walk(call) if isinstance(child, ast.Name))
            check_enforced = check_enforced or any(
                keyword.arg == "check"
                and isinstance(keyword.value, ast.Constant)
                and keyword.value.value is True
                for keyword in call.keywords
            )
            if result_name:
                result_names.add(result_name)
        if not execution_lines:
            continue
        first_execution = min(execution_lines)
        if check_enforced or any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_process(node, result_names)
            for node in ast.walk(test)
        ):
            observes_process = True
        if any(
            isinstance(node, ast.Assert)
            and node.lineno > first_execution
            and _assert_observes_output(node, command_names)
            for node in ast.walk(test)
        ):
            observes_output = True
    return _TestEvidence(has_marker, executes, observes_process, )

mutants_x__test_file_evidence__mutmut['_mutmut_orig'] = x__test_file_evidence__mutmut_orig # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_1'] = x__test_file_evidence__mutmut_1 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_2'] = x__test_file_evidence__mutmut_2 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_3'] = x__test_file_evidence__mutmut_3 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_4'] = x__test_file_evidence__mutmut_4 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_5'] = x__test_file_evidence__mutmut_5 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_6'] = x__test_file_evidence__mutmut_6 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_7'] = x__test_file_evidence__mutmut_7 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_8'] = x__test_file_evidence__mutmut_8 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_9'] = x__test_file_evidence__mutmut_9 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_10'] = x__test_file_evidence__mutmut_10 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_11'] = x__test_file_evidence__mutmut_11 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_12'] = x__test_file_evidence__mutmut_12 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_13'] = x__test_file_evidence__mutmut_13 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_14'] = x__test_file_evidence__mutmut_14 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_15'] = x__test_file_evidence__mutmut_15 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_16'] = x__test_file_evidence__mutmut_16 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_17'] = x__test_file_evidence__mutmut_17 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_18'] = x__test_file_evidence__mutmut_18 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_19'] = x__test_file_evidence__mutmut_19 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_20'] = x__test_file_evidence__mutmut_20 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_21'] = x__test_file_evidence__mutmut_21 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_22'] = x__test_file_evidence__mutmut_22 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_23'] = x__test_file_evidence__mutmut_23 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_24'] = x__test_file_evidence__mutmut_24 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_25'] = x__test_file_evidence__mutmut_25 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_26'] = x__test_file_evidence__mutmut_26 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_27'] = x__test_file_evidence__mutmut_27 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_28'] = x__test_file_evidence__mutmut_28 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_29'] = x__test_file_evidence__mutmut_29 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_30'] = x__test_file_evidence__mutmut_30 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_31'] = x__test_file_evidence__mutmut_31 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_32'] = x__test_file_evidence__mutmut_32 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_33'] = x__test_file_evidence__mutmut_33 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_34'] = x__test_file_evidence__mutmut_34 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_35'] = x__test_file_evidence__mutmut_35 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_36'] = x__test_file_evidence__mutmut_36 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_37'] = x__test_file_evidence__mutmut_37 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_38'] = x__test_file_evidence__mutmut_38 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_39'] = x__test_file_evidence__mutmut_39 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_40'] = x__test_file_evidence__mutmut_40 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_41'] = x__test_file_evidence__mutmut_41 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_42'] = x__test_file_evidence__mutmut_42 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_43'] = x__test_file_evidence__mutmut_43 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_44'] = x__test_file_evidence__mutmut_44 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_45'] = x__test_file_evidence__mutmut_45 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_46'] = x__test_file_evidence__mutmut_46 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_47'] = x__test_file_evidence__mutmut_47 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_48'] = x__test_file_evidence__mutmut_48 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_49'] = x__test_file_evidence__mutmut_49 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_50'] = x__test_file_evidence__mutmut_50 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_51'] = x__test_file_evidence__mutmut_51 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_52'] = x__test_file_evidence__mutmut_52 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_53'] = x__test_file_evidence__mutmut_53 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_54'] = x__test_file_evidence__mutmut_54 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_55'] = x__test_file_evidence__mutmut_55 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_56'] = x__test_file_evidence__mutmut_56 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_57'] = x__test_file_evidence__mutmut_57 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_58'] = x__test_file_evidence__mutmut_58 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_59'] = x__test_file_evidence__mutmut_59 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_60'] = x__test_file_evidence__mutmut_60 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_61'] = x__test_file_evidence__mutmut_61 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_62'] = x__test_file_evidence__mutmut_62 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_63'] = x__test_file_evidence__mutmut_63 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_64'] = x__test_file_evidence__mutmut_64 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_65'] = x__test_file_evidence__mutmut_65 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_66'] = x__test_file_evidence__mutmut_66 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_67'] = x__test_file_evidence__mutmut_67 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_68'] = x__test_file_evidence__mutmut_68 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_69'] = x__test_file_evidence__mutmut_69 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_70'] = x__test_file_evidence__mutmut_70 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_71'] = x__test_file_evidence__mutmut_71 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_72'] = x__test_file_evidence__mutmut_72 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_73'] = x__test_file_evidence__mutmut_73 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_74'] = x__test_file_evidence__mutmut_74 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_75'] = x__test_file_evidence__mutmut_75 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_76'] = x__test_file_evidence__mutmut_76 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_77'] = x__test_file_evidence__mutmut_77 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_78'] = x__test_file_evidence__mutmut_78 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_79'] = x__test_file_evidence__mutmut_79 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_80'] = x__test_file_evidence__mutmut_80 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_81'] = x__test_file_evidence__mutmut_81 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_82'] = x__test_file_evidence__mutmut_82 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_83'] = x__test_file_evidence__mutmut_83 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_84'] = x__test_file_evidence__mutmut_84 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_85'] = x__test_file_evidence__mutmut_85 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_86'] = x__test_file_evidence__mutmut_86 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_87'] = x__test_file_evidence__mutmut_87 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_88'] = x__test_file_evidence__mutmut_88 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_89'] = x__test_file_evidence__mutmut_89 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_90'] = x__test_file_evidence__mutmut_90 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_91'] = x__test_file_evidence__mutmut_91 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_92'] = x__test_file_evidence__mutmut_92 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_93'] = x__test_file_evidence__mutmut_93 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_94'] = x__test_file_evidence__mutmut_94 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_95'] = x__test_file_evidence__mutmut_95 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_96'] = x__test_file_evidence__mutmut_96 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_97'] = x__test_file_evidence__mutmut_97 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_98'] = x__test_file_evidence__mutmut_98 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_99'] = x__test_file_evidence__mutmut_99 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_100'] = x__test_file_evidence__mutmut_100 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_101'] = x__test_file_evidence__mutmut_101 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_102'] = x__test_file_evidence__mutmut_102 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_103'] = x__test_file_evidence__mutmut_103 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_104'] = x__test_file_evidence__mutmut_104 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_105'] = x__test_file_evidence__mutmut_105 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_106'] = x__test_file_evidence__mutmut_106 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_107'] = x__test_file_evidence__mutmut_107 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_108'] = x__test_file_evidence__mutmut_108 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_109'] = x__test_file_evidence__mutmut_109 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_110'] = x__test_file_evidence__mutmut_110 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_111'] = x__test_file_evidence__mutmut_111 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_112'] = x__test_file_evidence__mutmut_112 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_113'] = x__test_file_evidence__mutmut_113 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_114'] = x__test_file_evidence__mutmut_114 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_115'] = x__test_file_evidence__mutmut_115 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_116'] = x__test_file_evidence__mutmut_116 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_117'] = x__test_file_evidence__mutmut_117 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_118'] = x__test_file_evidence__mutmut_118 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_119'] = x__test_file_evidence__mutmut_119 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_120'] = x__test_file_evidence__mutmut_120 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_121'] = x__test_file_evidence__mutmut_121 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_122'] = x__test_file_evidence__mutmut_122 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_123'] = x__test_file_evidence__mutmut_123 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_124'] = x__test_file_evidence__mutmut_124 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_125'] = x__test_file_evidence__mutmut_125 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_126'] = x__test_file_evidence__mutmut_126 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_127'] = x__test_file_evidence__mutmut_127 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_128'] = x__test_file_evidence__mutmut_128 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_129'] = x__test_file_evidence__mutmut_129 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_130'] = x__test_file_evidence__mutmut_130 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_131'] = x__test_file_evidence__mutmut_131 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_132'] = x__test_file_evidence__mutmut_132 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_133'] = x__test_file_evidence__mutmut_133 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_134'] = x__test_file_evidence__mutmut_134 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_135'] = x__test_file_evidence__mutmut_135 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_136'] = x__test_file_evidence__mutmut_136 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_137'] = x__test_file_evidence__mutmut_137 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_138'] = x__test_file_evidence__mutmut_138 # type: ignore # mutmut generated
mutants_x__test_file_evidence__mutmut['x__test_file_evidence__mutmut_139'] = x__test_file_evidence__mutmut_139 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁBehaviouralEvidenceǁfrom_config__mutmut: MutantDict = {}  # type: ignore
mutants_xǁBehaviouralEvidenceǁfile_has_violation__mutmut: MutantDict = {}  # type: ignore
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut: MutantDict = {}  # type: ignore
mutants_xǁBehaviouralEvidenceǁrun__mutmut: MutantDict = {}  # type: ignore


class BehaviouralEvidence(FitnessRule):
    """Hard gate for declared critical-surface behavioural evidence."""

    name = "behavioural-evidence"
    remediation = REMEDIATION

    @_mutmut_mutated(mutants_xǁBehaviouralEvidenceǁ__init____mutmut)
    def __init__(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(repo_root=repo_root)
        self.config = dict(config)
        self.active = bool(config)

    def xǁBehaviouralEvidenceǁ__init____mutmut_orig(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(repo_root=repo_root)
        self.config = dict(config)
        self.active = bool(config)

    def xǁBehaviouralEvidenceǁ__init____mutmut_1(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(repo_root=None)
        self.config = dict(config)
        self.active = bool(config)

    def xǁBehaviouralEvidenceǁ__init____mutmut_2(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(repo_root=repo_root)
        self.config = None
        self.active = bool(config)

    def xǁBehaviouralEvidenceǁ__init____mutmut_3(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(repo_root=repo_root)
        self.config = dict(None)
        self.active = bool(config)

    def xǁBehaviouralEvidenceǁ__init____mutmut_4(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(repo_root=repo_root)
        self.config = dict(config)
        self.active = None

    def xǁBehaviouralEvidenceǁ__init____mutmut_5(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(repo_root=repo_root)
        self.config = dict(config)
        self.active = bool(None)

    @classmethod
    @_mutmut_mutated(mutants_xǁBehaviouralEvidenceǁfrom_config__mutmut, is_classmethod = True)
    def from_config(cls, config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
        return cls(cast(Mapping[str, object], config), repo_root=repo_root)

    @classmethod
    def xǁBehaviouralEvidenceǁfrom_config__mutmut_orig(cls, config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
        return cls(cast(Mapping[str, object], config), repo_root=repo_root)

    @classmethod
    def xǁBehaviouralEvidenceǁfrom_config__mutmut_1(cls, config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
        return cls(None, repo_root=repo_root)

    @classmethod
    def xǁBehaviouralEvidenceǁfrom_config__mutmut_2(cls, config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
        return cls(cast(Mapping[str, object], config), repo_root=None)

    @classmethod
    def xǁBehaviouralEvidenceǁfrom_config__mutmut_3(cls, config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
        return cls(repo_root=repo_root)

    @classmethod
    def xǁBehaviouralEvidenceǁfrom_config__mutmut_4(cls, config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
        return cls(cast(Mapping[str, object], config), )

    @classmethod
    def xǁBehaviouralEvidenceǁfrom_config__mutmut_5(cls, config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
        return cls(cast(None, config), repo_root=repo_root)

    @classmethod
    def xǁBehaviouralEvidenceǁfrom_config__mutmut_6(cls, config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
        return cls(cast(Mapping[str, object], None), repo_root=repo_root)

    @classmethod
    def xǁBehaviouralEvidenceǁfrom_config__mutmut_7(cls, config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
        return cls(cast(config), repo_root=repo_root)

    @classmethod
    def xǁBehaviouralEvidenceǁfrom_config__mutmut_8(cls, config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
        return cls(cast(Mapping[str, object], ), repo_root=repo_root)

    @_mutmut_mutated(mutants_xǁBehaviouralEvidenceǁfile_has_violation__mutmut)
    def file_has_violation(self, path: Path) -> bool:
        return False

    def xǁBehaviouralEvidenceǁfile_has_violation__mutmut_orig(self, path: Path) -> bool:
        return False

    def xǁBehaviouralEvidenceǁfile_has_violation__mutmut_1(self, path: Path) -> bool:
        return True

    @_mutmut_mutated(mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut)
    def collect_findings(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_orig(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_1(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_2(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = None
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_3(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = None
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_4(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(None)
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_5(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get(None))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_6(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("XXbehaviour_markersXX"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_7(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("BEHAVIOUR_MARKERS"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_8(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = None
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_9(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(None)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_10(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values and () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_11(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) or value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_12(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_13(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                None
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_14(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    None,
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_15(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    None,
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_16(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    None,
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_17(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    None,
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_18(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    None,
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_19(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_20(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_21(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_22(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_23(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_24(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "XXpyproject.tomlXX",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_25(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "PYPROJECT.TOML",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_26(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "XX/behaviour_markersXX",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_27(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/BEHAVIOUR_MARKERS",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_28(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "XXinvalid-behaviour-markersXX",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_29(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "INVALID-BEHAVIOUR-MARKERS",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_30(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "XXbehaviour_markers must contain at least one non-empty pytest markerXX",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_31(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "BEHAVIOUR_MARKERS MUST CONTAIN AT LEAST ONE NON-EMPTY PYTEST MARKER",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_32(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "XXdeclare the integration, e2e, journey or equivalent markers that carry real evidenceXX",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_33(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "DECLARE THE INTEGRATION, E2E, JOURNEY OR EQUIVALENT MARKERS THAT CARRY REAL EVIDENCE",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_34(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = None
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_35(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = None
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_36(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(None)
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_37(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get(None))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_38(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("XXsurface_globsXX"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_39(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("SURFACE_GLOBS"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_40(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(None):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_41(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values and ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_42(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = None
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_43(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(None)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_44(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is not None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_45(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    None
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_46(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        None,
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_47(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        None,
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_48(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        None,
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_49(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        None,
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_50(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        None,
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_51(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_52(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_53(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_54(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_55(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_56(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "XXpyproject.tomlXX",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_57(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "PYPROJECT.TOML",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_58(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "XXinvalid-surface-globXX",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_59(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "INVALID-SURFACE-GLOB",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_60(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "XXsurface glob must be a non-empty repo-relative POSIX pathXX",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_61(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative posix path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_62(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "SURFACE GLOB MUST BE A NON-EMPTY REPO-RELATIVE POSIX PATH",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_63(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "XXuse a repo-relative glob without '..' or a leading slashXX",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_64(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "USE A REPO-RELATIVE GLOB WITHOUT '..' OR A LEADING SLASH",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_65(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                break
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_66(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = None
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_67(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(None).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_68(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(None)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_69(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_70(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    None
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_71(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        None,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_72(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        None,
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_73(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        None,
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_74(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        None,
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_75(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        None,
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_76(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_77(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_78(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_79(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_80(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_81(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "XXunmatched-surface-globXX",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_82(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "UNMATCHED-SURFACE-GLOB",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_83(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "XXcorrect the glob or restore the required critical surfaceXX",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_84(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "CORRECT THE GLOB OR RESTORE THE REQUIRED CRITICAL SURFACE",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_85(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(None)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_86(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = None
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_87(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(None)
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_88(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get(None))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_89(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("XXclaimsXX"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_90(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("CLAIMS"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_91(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = None
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_92(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = None
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_93(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None and not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_94(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is not None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_95(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_96(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                None
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_97(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    None,
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_98(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    None,
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_99(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    None,
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_100(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    None,
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_101(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    None,
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_102(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_103(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_104(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_105(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_106(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_107(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "XXpyproject.tomlXX",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_108(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "PYPROJECT.TOML",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_109(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "XX/claimsXX",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_110(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/CLAIMS",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_111(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "XXmissing-evidence-claimsXX",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_112(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "MISSING-EVIDENCE-CLAIMS",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_113(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "XXactive behavioural evidence configuration requires at least one claimXX",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_114(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "ACTIVE BEHAVIOURAL EVIDENCE CONFIGURATION REQUIRES AT LEAST ONE CLAIM",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_115(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "XXmap each critical surface to its executable and behavioural testsXX",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_116(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "MAP EACH CRITICAL SURFACE TO ITS EXECUTABLE AND BEHAVIOURAL TESTS",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_117(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = None

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_118(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(None):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_119(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = None
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_120(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_121(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    None
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_122(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        None,
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_123(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        None,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_124(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        None,
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_125(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        None,
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_126(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        None,
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_127(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_128(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_129(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_130(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_131(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_132(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "XXpyproject.tomlXX",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_133(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "PYPROJECT.TOML",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_134(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "XXinvalid-evidence-claimXX",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_135(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "INVALID-EVIDENCE-CLAIM",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_136(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "XXeach claim must be a mappingXX",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_137(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "EACH CLAIM MUST BE A MAPPING",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_138(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "XXdeclare id, surfaces, tests and executables for the claimXX",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_139(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "DECLARE ID, SURFACES, TESTS AND EXECUTABLES FOR THE CLAIM",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_140(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                break
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_141(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = None
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_142(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(None, raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_143(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], None)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_144(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_145(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], )
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_146(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = None
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_147(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get(None)
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_148(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("XXidXX")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_149(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("ID")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_150(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() and claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_151(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) and not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_152(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_153(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_154(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id not in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_155(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    None
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_156(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        None,
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_157(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        None,
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_158(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        None,
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_159(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        None,
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_160(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        None,
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_161(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_162(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_163(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_164(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_165(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_166(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "XXpyproject.tomlXX",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_167(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "PYPROJECT.TOML",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_168(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "XXinvalid-evidence-claim-idXX",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_169(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "INVALID-EVIDENCE-CLAIM-ID",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_170(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "XXclaim id must be a unique non-empty stringXX",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_171(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "CLAIM ID MUST BE A UNIQUE NON-EMPTY STRING",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_172(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "XXgive each behavioural claim one stable unique idXX",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_173(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "GIVE EACH BEHAVIOURAL CLAIM ONE STABLE UNIQUE ID",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_174(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = None
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_175(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(None)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_176(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = None
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_177(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(None)
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_178(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get(None))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_179(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("XXsurfacesXX"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_180(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("SURFACES"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_181(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_182(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    None
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_183(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        None,
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_184(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        None,
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_185(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        None,
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_186(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        None,
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_187(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        None,
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_188(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_189(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_190(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_191(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_192(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_193(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "XXpyproject.tomlXX",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_194(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "PYPROJECT.TOML",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_195(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "XXmissing-claim-surfacesXX",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_196(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "MISSING-CLAIM-SURFACES",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_197(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "XXname every exact critical surface proved by this claimXX",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_198(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "NAME EVERY EXACT CRITICAL SURFACE PROVED BY THIS CLAIM",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_199(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(None):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_200(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values and ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_201(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = None
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_202(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(None)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_203(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is not None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_204(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        None
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_205(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            None,
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_206(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            None,
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_207(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            None,
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_208(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            None,
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_209(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            None,
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_210(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_211(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_212(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_213(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_214(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_215(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "XXpyproject.tomlXX",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_216(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "PYPROJECT.TOML",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_217(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "XXinvalid-claimed-surfaceXX",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_218(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "INVALID-CLAIMED-SURFACE",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_219(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "XXclaimed surface must be a repo-relative POSIX pathXX",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_220(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative posix path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_221(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "CLAIMED SURFACE MUST BE A REPO-RELATIVE POSIX PATH",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_222(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "XXdeclare the exact repository path of the critical surfaceXX",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_223(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "DECLARE THE EXACT REPOSITORY PATH OF THE CRITICAL SURFACE",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_224(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    break
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_225(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(None)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_226(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_227(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root * surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_228(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        None
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_229(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            None,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_230(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            None,
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_231(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            None,
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_232(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            None,
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_233(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            None,
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_234(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_235(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_236(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_237(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_238(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_239(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "XXmissing-claimed-surfaceXX",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_240(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "MISSING-CLAIMED-SURFACE",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_241(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "XXrestore the surface or remove the stale claimXX",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_242(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "RESTORE THE SURFACE OR REMOVE THE STALE CLAIM",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_243(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = None
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_244(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(None)
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_245(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get(None))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_246(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("XXtestsXX"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_247(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("TESTS"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_248(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_249(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    None
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_250(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        None,
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_251(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        None,
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_252(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        None,
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_253(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        None,
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_254(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        None,
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_255(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_256(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_257(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_258(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_259(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_260(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "XXpyproject.tomlXX",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_261(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "PYPROJECT.TOML",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_262(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "XXmissing-claim-testsXX",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_263(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "MISSING-CLAIM-TESTS",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_264(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "XXname the integration or E2E tests that execute this claimXX",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_265(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or e2e tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_266(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "NAME THE INTEGRATION OR E2E TESTS THAT EXECUTE THIS CLAIM",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_267(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = None
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_268(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(None):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_269(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values and ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_270(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = None
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_271(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(None)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_272(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is not None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_273(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        None
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_274(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            None,
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_275(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            None,
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_276(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            None,
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_277(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            None,
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_278(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            None,
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_279(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_280(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_281(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_282(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_283(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_284(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "XXpyproject.tomlXX",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_285(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "PYPROJECT.TOML",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_286(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "XXinvalid-evidence-testXX",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_287(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "INVALID-EVIDENCE-TEST",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_288(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "XXevidence test must be a repo-relative POSIX pathXX",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_289(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative posix path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_290(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "EVIDENCE TEST MUST BE A REPO-RELATIVE POSIX PATH",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_291(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "XXdeclare an exact Python test pathXX",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_292(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_293(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "DECLARE AN EXACT PYTHON TEST PATH",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_294(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    break
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_295(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = None
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_296(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root * test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_297(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_298(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        None
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_299(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            None,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_300(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            None,
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_301(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            None,
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_302(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            None,
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_303(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            None,
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_304(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_305(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_306(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_307(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_308(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_309(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "XXmissing-evidence-testXX",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_310(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "MISSING-EVIDENCE-TEST",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_311(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "XXadd the behavioural test or remove the stale claimXX",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_312(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "ADD THE BEHAVIOURAL TEST OR REMOVE THE STALE CLAIM",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_313(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    break
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_314(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(None)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_315(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = None
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_316(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(None)
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_317(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get(None))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_318(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("XXexecutablesXX"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_319(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("EXECUTABLES"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_320(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_321(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    None
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_322(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        None,
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_323(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        None,
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_324(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        None,
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_325(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        None,
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_326(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        None,
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_327(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_328(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_329(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_330(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_331(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_332(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "XXpyproject.tomlXX",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_333(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "PYPROJECT.TOML",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_334(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "XXmissing-claim-executablesXX",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_335(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "MISSING-CLAIM-EXECUTABLES",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_336(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "XXname the real executable the behavioural test must invokeXX",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_337(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "NAME THE REAL EXECUTABLE THE BEHAVIOURAL TEST MUST INVOKE",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_338(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(None):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_339(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values and ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_340(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = None
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_341(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(None)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_342(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is not None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_343(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        None
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_344(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            None,
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_345(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            None,
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_346(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            None,
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_347(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            None,
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_348(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            None,
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_349(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_350(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_351(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_352(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_353(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_354(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "XXpyproject.tomlXX",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_355(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "PYPROJECT.TOML",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_356(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "XXinvalid-evidence-executableXX",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_357(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "INVALID-EVIDENCE-EXECUTABLE",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_358(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "XXevidence executable must be a repo-relative POSIX pathXX",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_359(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative posix path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_360(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "EVIDENCE EXECUTABLE MUST BE A REPO-RELATIVE POSIX PATH",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_361(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "XXdeclare the exact executable path used by the critical surfaceXX",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_362(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "DECLARE THE EXACT EXECUTABLE PATH USED BY THE CRITICAL SURFACE",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_363(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    break
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_364(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_365(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    break
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_366(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = None
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_367(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(None, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_368(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=None, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_369(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=None) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_370(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_371(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_372(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, ) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_373(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_374(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(None):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_375(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        None
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_376(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            None,
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_377(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            None,
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_378(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            None,
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_379(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            None,
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_380(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            None,
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_381(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_382(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_383(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_384(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_385(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_386(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(None) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_387(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[1].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_388(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "XXpyproject.tomlXX",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_389(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "PYPROJECT.TOML",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_390(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "XXmissing-behaviour-markerXX",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_391(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "MISSING-BEHAVIOUR-MARKER",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_392(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "XXmark the real behavioural test as integration, e2e, journey or the configured equivalentXX",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_393(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "MARK THE REAL BEHAVIOURAL TEST AS INTEGRATION, E2E, JOURNEY OR THE CONFIGURED EQUIVALENT",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_394(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_395(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(None):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_396(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        None
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_397(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            None,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_398(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            None,
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_399(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            None,
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_400(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            None,
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_401(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            None,
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_402(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_403(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_404(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_405(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_406(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_407(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "XXmissing-executable-evidenceXX",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_408(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "MISSING-EXECUTABLE-EVIDENCE",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_409(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "XXinvoke the declared executable through subprocess in a configured behavioural testXX",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_410(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "INVOKE THE DECLARED EXECUTABLE THROUGH SUBPROCESS IN A CONFIGURED BEHAVIOURAL TEST",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_411(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_412(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(None):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_413(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes or item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_414(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            None
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_415(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                None,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_416(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                None,
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_417(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                None,
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_418(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                None,
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_419(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                None,
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_420(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_421(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_422(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_423(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_424(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_425(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "XXmissing-process-observationXX",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_426(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "MISSING-PROCESS-OBSERVATION",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_427(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "XXassert returncode/stdout/stderr or invoke subprocess with check=TrueXX",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_428(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=true",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_429(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "ASSERT RETURNCODE/STDOUT/STDERR OR INVOKE SUBPROCESS WITH CHECK=TRUE",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_430(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(None):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_431(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes or item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_432(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        break
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_433(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        None
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_434(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            None,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_435(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            None,
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_436(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            None,
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_437(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            None,
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_438(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            None,
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_439(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_440(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_441(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_442(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_443(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_444(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "XXmissing-output-observationXX",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_445(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "MISSING-OUTPUT-OBSERVATION",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_446(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "XXassert a produced file, response or retained receipt passed to the executableXX",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_447(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "ASSERT A PRODUCED FILE, RESPONSE OR RETAINED RECEIPT PASSED TO THE EXECUTABLE",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_448(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(None):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_449(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces + claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_450(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                None
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_451(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    None,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_452(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    None,
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_453(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    None,
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_454(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    None,
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_455(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    None,
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_456(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_457(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_458(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_459(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_460(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_461(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "XX/surface_globsXX",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_462(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/SURFACE_GLOBS",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_463(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "XXunclaimed-critical-surfaceXX",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_464(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "UNCLAIMED-CRITICAL-SURFACE",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_465(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "XXcritical surface has no behavioural evidence claimXX",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_466(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "CRITICAL SURFACE HAS NO BEHAVIOURAL EVIDENCE CLAIM",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_467(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "XXmap the surface to the executable and test that prove its behaviourXX",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_468(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "MAP THE SURFACE TO THE EXECUTABLE AND TEST THAT PROVE ITS BEHAVIOUR",
                )
            )
        return tuple(sorted(set(findings)))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_469(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(None)

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_470(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(None))

    def xǁBehaviouralEvidenceǁcollect_findings__mutmut_471(self) -> tuple[BehaviouralEvidenceFinding, ...]:
        if not self.active:
            return ()
        findings: list[BehaviouralEvidenceFinding] = []
        marker_values = _sequence(self.config.get("behaviour_markers"))
        markers = frozenset(value for value in marker_values or () if isinstance(value, str) and value)
        if not markers:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/behaviour_markers",
                    "invalid-behaviour-markers",
                    "behaviour_markers must contain at least one non-empty pytest marker",
                    "declare the integration, e2e, journey or equivalent markers that carry real evidence",
                )
            )

        matched_surfaces: set[str] = set()
        glob_values = _sequence(self.config.get("surface_globs"))
        for index, raw_glob in enumerate(glob_values or ()):
            pattern = _safe_relative_path(raw_glob)
            if pattern is None:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"/surface_globs/{index}",
                        "invalid-surface-glob",
                        "surface glob must be a non-empty repo-relative POSIX path",
                        "use a repo-relative glob without '..' or a leading slash",
                    )
                )
                continue
            matches = {path.relative_to(self._repo_root).as_posix() for path in self._repo_root.glob(pattern)}
            if not matches:
                findings.append(
                    _finding(
                        pattern,
                        f"/surface_globs/{index}",
                        "unmatched-surface-glob",
                        f"critical surface glob {pattern!r} matches no files",
                        "correct the glob or restore the required critical surface",
                    )
                )
            matched_surfaces.update(matches)

        claims_raw = _sequence(self.config.get("claims"))
        claimed_surfaces: set[str] = set()
        seen_ids: set[str] = set()
        if claims_raw is None or not claims_raw:
            findings.append(
                _finding(
                    "pyproject.toml",
                    "/claims",
                    "missing-evidence-claims",
                    "active behavioural evidence configuration requires at least one claim",
                    "map each critical surface to its executable and behavioural tests",
                )
            )
            claims_raw = ()

        for index, raw_claim in enumerate(claims_raw):
            pointer = f"/claims/{index}"
            if not isinstance(raw_claim, Mapping):
                findings.append(
                    _finding(
                        "pyproject.toml",
                        pointer,
                        "invalid-evidence-claim",
                        "each claim must be a mapping",
                        "declare id, surfaces, tests and executables for the claim",
                    )
                )
                continue
            claim = cast(Mapping[str, object], raw_claim)
            claim_id = claim.get("id")
            if not isinstance(claim_id, str) or not claim_id.strip() or claim_id in seen_ids:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/id",
                        "invalid-evidence-claim-id",
                        "claim id must be a unique non-empty string",
                        "give each behavioural claim one stable unique id",
                    )
                )
                claim_id = f"claim-{index}"
            seen_ids.add(claim_id)

            surface_values = _sequence(claim.get("surfaces"))
            if not surface_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/surfaces",
                        "missing-claim-surfaces",
                        f"claim {claim_id!r} has no critical surfaces",
                        "name every exact critical surface proved by this claim",
                    )
                )
            for surface_index, raw_surface in enumerate(surface_values or ()):
                surface = _safe_relative_path(raw_surface)
                if surface is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/surfaces/{surface_index}",
                            "invalid-claimed-surface",
                            "claimed surface must be a repo-relative POSIX path",
                            "declare the exact repository path of the critical surface",
                        )
                    )
                    continue
                claimed_surfaces.add(surface)
                if not (self._repo_root / surface).is_file():
                    findings.append(
                        _finding(
                            surface,
                            f"{pointer}/surfaces/{surface_index}",
                            "missing-claimed-surface",
                            f"claim {claim_id!r} names a surface that does not exist",
                            "restore the surface or remove the stale claim",
                        )
                    )

            test_values = _sequence(claim.get("tests"))
            if not test_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/tests",
                        "missing-claim-tests",
                        f"claim {claim_id!r} has no behavioural tests",
                        "name the integration or E2E tests that execute this claim",
                    )
                )
            test_paths: list[Path] = []
            for test_index, raw_test in enumerate(test_values or ()):
                test_rel = _safe_relative_path(raw_test)
                if test_rel is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/tests/{test_index}",
                            "invalid-evidence-test",
                            "evidence test must be a repo-relative POSIX path",
                            "declare an exact Python test path",
                        )
                    )
                    continue
                test_path = self._repo_root / test_rel
                if not test_path.is_file():
                    findings.append(
                        _finding(
                            test_rel,
                            f"{pointer}/tests/{test_index}",
                            "missing-evidence-test",
                            f"claim {claim_id!r} names a test that does not exist",
                            "add the behavioural test or remove the stale claim",
                        )
                    )
                    continue
                test_paths.append(test_path)

            executable_values = _sequence(claim.get("executables"))
            if not executable_values:
                findings.append(
                    _finding(
                        "pyproject.toml",
                        f"{pointer}/executables",
                        "missing-claim-executables",
                        f"claim {claim_id!r} has no executable boundary",
                        "name the real executable the behavioural test must invoke",
                    )
                )
            for executable_index, raw_executable in enumerate(executable_values or ()):
                executable = _safe_relative_path(raw_executable)
                if executable is None:
                    findings.append(
                        _finding(
                            "pyproject.toml",
                            f"{pointer}/executables/{executable_index}",
                            "invalid-evidence-executable",
                            "evidence executable must be a repo-relative POSIX path",
                            "declare the exact executable path used by the critical surface",
                        )
                    )
                    continue
                if not test_paths:
                    continue
                evidence = [
                    _test_file_evidence(path, executable=executable, markers=markers) for path in test_paths
                ]
                if not any(item.has_marker for item in evidence):
                    findings.append(
                        _finding(
                            test_paths[0].relative_to(self._repo_root) if test_paths else "pyproject.toml",
                            f"{pointer}/tests",
                            "missing-behaviour-marker",
                            f"claim {claim_id!r} has no test using a configured behavioural marker",
                            "mark the real behavioural test as integration, e2e, journey or the configured equivalent",
                        )
                    )
                elif not any(item.executes for item in evidence):
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-executable-evidence",
                            f"claim {claim_id!r} never executes {executable}",
                            "invoke the declared executable through subprocess in a configured behavioural test",
                        )
                    )
                else:
                    if not any(item.executes and item.observes_process for item in evidence):
                        findings.append(
                            _finding(
                                executable,
                                f"{pointer}/executables/{executable_index}",
                                "missing-process-observation",
                                f"claim {claim_id!r} executes {executable} without checking process success",
                                "assert returncode/stdout/stderr or invoke subprocess with check=True",
                            )
                        )
                    if any(item.executes and item.observes_output for item in evidence):
                        continue
                    findings.append(
                        _finding(
                            executable,
                            f"{pointer}/executables/{executable_index}",
                            "missing-output-observation",
                            f"claim {claim_id!r} executes {executable} without asserting a passed output",
                            "assert a produced file, response or retained receipt passed to the executable",
                        )
                    )

        for surface in sorted(matched_surfaces - claimed_surfaces):
            findings.append(
                _finding(
                    surface,
                    "/surface_globs",
                    "unclaimed-critical-surface",
                    "critical surface has no behavioural evidence claim",
                    "map the surface to the executable and test that prove its behaviour",
                )
            )
        return tuple(sorted(set(None)))

    @_mutmut_mutated(mutants_xǁBehaviouralEvidenceǁrun__mutmut)
    def run(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_orig(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_1(self) -> int:
        findings = None
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_2(self) -> int:
        findings = self.collect_findings()
        if findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_3(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 1
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_4(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                None,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_5(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                None,
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_6(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                None,
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_7(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_8(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_9(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_10(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(None, file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_11(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=None)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_12(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_13(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", )
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_14(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(None, file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_15(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=None)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_16(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_17(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", )
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_18(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print(None, file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_19(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=None)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_20(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print(file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_21(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", )
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_22(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("XXnext: run the declared behavioural test, then re-run this checkXX", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_23(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("NEXT: RUN THE DECLARED BEHAVIOURAL TEST, THEN RE-RUN THIS CHECK", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_24(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print(None, file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_25(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=None)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_26(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print(file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_27(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", )
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_28(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("XXrun: python -m tc_fitness.core_checks.behavioural_evidenceXX", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_29(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("RUN: PYTHON -M TC_FITNESS.CORE_CHECKS.BEHAVIOURAL_EVIDENCE", file=sys.stderr)
        return 1

    def xǁBehaviouralEvidenceǁrun__mutmut_30(self) -> int:
        findings = self.collect_findings()
        if not findings:
            return 0
        for finding in findings:
            report_finding(
                self.name,
                finding.source.as_posix(),
                f"{finding.pointer}: {finding.code}: {finding.message}; fix: {finding.fix}",
            )
            print(f"{finding.source}:{finding.pointer}: {finding.code}: {finding.message}", file=sys.stderr)
            print(f"fix: {finding.fix}", file=sys.stderr)
            print("next: run the declared behavioural test, then re-run this check", file=sys.stderr)
            print("run: python -m tc_fitness.core_checks.behavioural_evidence", file=sys.stderr)
        return 2

mutants_xǁBehaviouralEvidenceǁ__init____mutmut['_mutmut_orig'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁ__init____mutmut['xǁBehaviouralEvidenceǁ__init____mutmut_1'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁ__init____mutmut['xǁBehaviouralEvidenceǁ__init____mutmut_2'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁ__init____mutmut['xǁBehaviouralEvidenceǁ__init____mutmut_3'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁ__init____mutmut['xǁBehaviouralEvidenceǁ__init____mutmut_4'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁ__init____mutmut['xǁBehaviouralEvidenceǁ__init____mutmut_5'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁ__init____mutmut_5 # type: ignore # mutmut generated

mutants_xǁBehaviouralEvidenceǁfrom_config__mutmut['_mutmut_orig'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁfrom_config__mutmut_orig # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁfrom_config__mutmut['xǁBehaviouralEvidenceǁfrom_config__mutmut_1'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁfrom_config__mutmut_1 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁfrom_config__mutmut['xǁBehaviouralEvidenceǁfrom_config__mutmut_2'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁfrom_config__mutmut_2 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁfrom_config__mutmut['xǁBehaviouralEvidenceǁfrom_config__mutmut_3'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁfrom_config__mutmut_3 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁfrom_config__mutmut['xǁBehaviouralEvidenceǁfrom_config__mutmut_4'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁfrom_config__mutmut_4 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁfrom_config__mutmut['xǁBehaviouralEvidenceǁfrom_config__mutmut_5'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁfrom_config__mutmut_5 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁfrom_config__mutmut['xǁBehaviouralEvidenceǁfrom_config__mutmut_6'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁfrom_config__mutmut_6 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁfrom_config__mutmut['xǁBehaviouralEvidenceǁfrom_config__mutmut_7'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁfrom_config__mutmut_7 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁfrom_config__mutmut['xǁBehaviouralEvidenceǁfrom_config__mutmut_8'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁfrom_config__mutmut_8 # type: ignore # mutmut generated

mutants_xǁBehaviouralEvidenceǁfile_has_violation__mutmut['_mutmut_orig'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁfile_has_violation__mutmut_orig # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁfile_has_violation__mutmut['xǁBehaviouralEvidenceǁfile_has_violation__mutmut_1'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁfile_has_violation__mutmut_1 # type: ignore # mutmut generated

mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['_mutmut_orig'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_orig # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_1'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_1 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_2'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_2 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_3'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_3 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_4'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_4 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_5'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_5 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_6'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_6 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_7'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_7 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_8'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_8 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_9'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_9 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_10'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_10 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_11'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_11 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_12'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_12 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_13'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_13 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_14'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_14 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_15'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_15 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_16'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_16 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_17'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_17 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_18'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_18 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_19'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_19 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_20'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_20 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_21'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_21 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_22'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_22 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_23'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_23 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_24'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_24 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_25'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_25 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_26'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_26 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_27'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_27 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_28'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_28 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_29'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_29 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_30'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_30 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_31'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_31 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_32'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_32 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_33'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_33 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_34'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_34 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_35'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_35 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_36'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_36 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_37'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_37 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_38'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_38 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_39'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_39 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_40'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_40 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_41'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_41 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_42'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_42 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_43'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_43 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_44'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_44 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_45'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_45 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_46'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_46 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_47'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_47 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_48'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_48 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_49'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_49 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_50'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_50 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_51'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_51 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_52'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_52 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_53'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_53 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_54'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_54 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_55'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_55 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_56'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_56 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_57'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_57 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_58'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_58 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_59'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_59 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_60'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_60 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_61'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_61 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_62'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_62 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_63'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_63 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_64'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_64 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_65'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_65 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_66'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_66 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_67'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_67 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_68'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_68 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_69'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_69 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_70'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_70 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_71'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_71 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_72'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_72 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_73'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_73 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_74'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_74 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_75'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_75 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_76'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_76 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_77'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_77 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_78'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_78 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_79'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_79 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_80'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_80 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_81'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_81 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_82'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_82 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_83'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_83 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_84'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_84 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_85'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_85 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_86'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_86 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_87'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_87 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_88'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_88 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_89'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_89 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_90'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_90 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_91'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_91 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_92'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_92 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_93'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_93 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_94'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_94 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_95'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_95 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_96'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_96 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_97'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_97 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_98'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_98 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_99'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_99 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_100'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_100 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_101'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_101 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_102'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_102 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_103'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_103 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_104'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_104 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_105'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_105 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_106'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_106 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_107'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_107 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_108'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_108 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_109'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_109 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_110'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_110 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_111'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_111 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_112'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_112 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_113'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_113 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_114'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_114 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_115'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_115 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_116'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_116 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_117'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_117 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_118'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_118 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_119'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_119 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_120'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_120 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_121'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_121 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_122'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_122 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_123'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_123 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_124'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_124 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_125'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_125 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_126'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_126 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_127'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_127 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_128'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_128 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_129'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_129 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_130'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_130 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_131'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_131 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_132'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_132 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_133'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_133 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_134'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_134 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_135'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_135 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_136'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_136 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_137'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_137 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_138'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_138 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_139'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_139 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_140'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_140 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_141'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_141 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_142'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_142 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_143'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_143 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_144'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_144 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_145'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_145 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_146'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_146 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_147'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_147 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_148'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_148 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_149'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_149 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_150'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_150 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_151'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_151 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_152'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_152 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_153'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_153 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_154'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_154 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_155'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_155 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_156'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_156 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_157'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_157 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_158'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_158 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_159'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_159 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_160'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_160 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_161'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_161 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_162'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_162 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_163'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_163 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_164'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_164 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_165'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_165 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_166'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_166 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_167'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_167 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_168'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_168 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_169'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_169 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_170'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_170 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_171'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_171 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_172'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_172 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_173'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_173 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_174'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_174 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_175'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_175 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_176'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_176 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_177'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_177 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_178'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_178 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_179'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_179 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_180'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_180 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_181'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_181 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_182'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_182 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_183'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_183 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_184'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_184 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_185'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_185 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_186'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_186 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_187'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_187 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_188'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_188 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_189'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_189 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_190'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_190 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_191'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_191 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_192'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_192 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_193'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_193 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_194'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_194 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_195'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_195 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_196'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_196 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_197'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_197 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_198'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_198 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_199'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_199 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_200'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_200 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_201'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_201 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_202'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_202 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_203'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_203 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_204'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_204 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_205'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_205 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_206'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_206 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_207'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_207 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_208'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_208 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_209'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_209 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_210'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_210 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_211'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_211 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_212'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_212 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_213'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_213 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_214'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_214 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_215'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_215 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_216'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_216 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_217'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_217 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_218'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_218 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_219'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_219 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_220'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_220 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_221'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_221 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_222'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_222 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_223'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_223 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_224'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_224 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_225'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_225 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_226'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_226 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_227'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_227 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_228'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_228 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_229'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_229 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_230'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_230 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_231'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_231 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_232'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_232 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_233'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_233 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_234'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_234 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_235'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_235 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_236'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_236 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_237'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_237 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_238'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_238 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_239'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_239 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_240'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_240 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_241'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_241 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_242'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_242 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_243'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_243 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_244'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_244 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_245'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_245 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_246'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_246 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_247'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_247 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_248'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_248 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_249'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_249 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_250'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_250 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_251'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_251 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_252'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_252 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_253'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_253 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_254'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_254 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_255'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_255 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_256'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_256 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_257'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_257 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_258'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_258 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_259'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_259 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_260'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_260 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_261'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_261 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_262'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_262 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_263'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_263 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_264'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_264 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_265'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_265 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_266'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_266 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_267'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_267 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_268'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_268 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_269'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_269 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_270'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_270 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_271'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_271 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_272'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_272 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_273'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_273 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_274'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_274 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_275'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_275 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_276'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_276 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_277'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_277 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_278'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_278 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_279'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_279 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_280'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_280 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_281'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_281 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_282'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_282 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_283'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_283 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_284'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_284 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_285'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_285 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_286'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_286 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_287'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_287 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_288'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_288 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_289'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_289 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_290'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_290 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_291'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_291 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_292'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_292 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_293'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_293 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_294'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_294 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_295'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_295 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_296'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_296 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_297'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_297 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_298'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_298 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_299'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_299 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_300'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_300 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_301'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_301 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_302'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_302 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_303'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_303 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_304'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_304 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_305'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_305 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_306'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_306 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_307'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_307 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_308'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_308 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_309'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_309 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_310'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_310 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_311'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_311 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_312'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_312 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_313'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_313 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_314'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_314 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_315'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_315 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_316'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_316 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_317'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_317 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_318'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_318 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_319'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_319 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_320'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_320 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_321'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_321 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_322'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_322 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_323'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_323 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_324'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_324 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_325'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_325 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_326'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_326 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_327'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_327 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_328'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_328 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_329'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_329 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_330'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_330 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_331'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_331 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_332'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_332 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_333'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_333 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_334'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_334 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_335'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_335 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_336'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_336 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_337'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_337 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_338'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_338 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_339'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_339 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_340'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_340 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_341'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_341 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_342'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_342 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_343'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_343 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_344'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_344 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_345'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_345 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_346'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_346 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_347'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_347 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_348'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_348 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_349'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_349 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_350'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_350 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_351'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_351 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_352'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_352 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_353'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_353 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_354'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_354 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_355'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_355 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_356'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_356 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_357'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_357 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_358'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_358 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_359'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_359 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_360'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_360 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_361'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_361 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_362'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_362 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_363'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_363 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_364'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_364 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_365'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_365 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_366'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_366 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_367'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_367 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_368'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_368 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_369'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_369 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_370'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_370 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_371'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_371 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_372'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_372 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_373'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_373 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_374'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_374 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_375'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_375 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_376'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_376 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_377'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_377 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_378'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_378 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_379'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_379 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_380'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_380 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_381'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_381 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_382'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_382 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_383'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_383 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_384'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_384 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_385'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_385 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_386'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_386 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_387'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_387 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_388'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_388 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_389'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_389 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_390'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_390 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_391'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_391 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_392'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_392 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_393'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_393 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_394'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_394 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_395'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_395 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_396'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_396 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_397'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_397 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_398'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_398 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_399'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_399 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_400'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_400 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_401'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_401 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_402'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_402 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_403'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_403 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_404'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_404 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_405'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_405 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_406'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_406 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_407'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_407 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_408'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_408 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_409'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_409 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_410'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_410 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_411'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_411 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_412'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_412 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_413'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_413 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_414'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_414 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_415'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_415 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_416'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_416 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_417'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_417 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_418'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_418 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_419'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_419 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_420'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_420 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_421'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_421 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_422'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_422 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_423'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_423 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_424'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_424 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_425'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_425 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_426'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_426 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_427'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_427 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_428'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_428 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_429'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_429 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_430'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_430 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_431'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_431 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_432'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_432 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_433'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_433 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_434'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_434 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_435'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_435 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_436'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_436 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_437'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_437 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_438'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_438 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_439'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_439 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_440'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_440 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_441'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_441 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_442'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_442 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_443'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_443 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_444'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_444 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_445'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_445 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_446'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_446 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_447'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_447 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_448'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_448 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_449'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_449 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_450'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_450 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_451'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_451 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_452'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_452 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_453'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_453 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_454'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_454 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_455'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_455 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_456'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_456 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_457'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_457 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_458'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_458 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_459'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_459 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_460'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_460 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_461'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_461 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_462'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_462 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_463'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_463 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_464'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_464 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_465'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_465 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_466'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_466 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_467'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_467 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_468'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_468 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_469'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_469 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_470'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_470 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁcollect_findings__mutmut['xǁBehaviouralEvidenceǁcollect_findings__mutmut_471'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁcollect_findings__mutmut_471 # type: ignore # mutmut generated

mutants_xǁBehaviouralEvidenceǁrun__mutmut['_mutmut_orig'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_orig # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_1'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_1 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_2'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_2 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_3'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_3 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_4'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_4 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_5'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_5 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_6'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_6 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_7'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_7 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_8'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_8 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_9'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_9 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_10'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_10 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_11'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_11 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_12'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_12 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_13'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_13 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_14'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_14 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_15'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_15 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_16'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_16 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_17'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_17 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_18'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_18 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_19'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_19 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_20'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_20 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_21'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_21 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_22'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_22 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_23'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_23 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_24'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_24 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_25'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_25 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_26'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_26 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_27'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_27 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_28'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_28 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_29'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_29 # type: ignore # mutmut generated
mutants_xǁBehaviouralEvidenceǁrun__mutmut['xǁBehaviouralEvidenceǁrun__mutmut_30'] = BehaviouralEvidence.xǁBehaviouralEvidenceǁrun__mutmut_30 # type: ignore # mutmut generated
mutants_x_build__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_build__mutmut)
def build(config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
    """Bind the hard behavioural-evidence rule to a consumer configuration."""
    return BehaviouralEvidence.from_config(config, repo_root=repo_root)


def x_build__mutmut_orig(config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
    """Bind the hard behavioural-evidence rule to a consumer configuration."""
    return BehaviouralEvidence.from_config(config, repo_root=repo_root)


def x_build__mutmut_1(config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
    """Bind the hard behavioural-evidence rule to a consumer configuration."""
    return BehaviouralEvidence.from_config(None, repo_root=repo_root)


def x_build__mutmut_2(config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
    """Bind the hard behavioural-evidence rule to a consumer configuration."""
    return BehaviouralEvidence.from_config(config, repo_root=None)


def x_build__mutmut_3(config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
    """Bind the hard behavioural-evidence rule to a consumer configuration."""
    return BehaviouralEvidence.from_config(repo_root=repo_root)


def x_build__mutmut_4(config: Mapping[str, Any], *, repo_root: Path | None = None) -> BehaviouralEvidence:
    """Bind the hard behavioural-evidence rule to a consumer configuration."""
    return BehaviouralEvidence.from_config(config, )

mutants_x_build__mutmut['_mutmut_orig'] = x_build__mutmut_orig # type: ignore # mutmut generated
mutants_x_build__mutmut['x_build__mutmut_1'] = x_build__mutmut_1 # type: ignore # mutmut generated
mutants_x_build__mutmut['x_build__mutmut_2'] = x_build__mutmut_2 # type: ignore # mutmut generated
mutants_x_build__mutmut['x_build__mutmut_3'] = x_build__mutmut_3 # type: ignore # mutmut generated
mutants_x_build__mutmut['x_build__mutmut_4'] = x_build__mutmut_4 # type: ignore # mutmut generated
mutants_x_main__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_main__mutmut)
def main(argv: list[str] | None = None) -> int:
    """Standalone probe; consumers normally invoke the configured catalogue."""
    del argv
    return build({}).run()


def x_main__mutmut_orig(argv: list[str] | None = None) -> int:
    """Standalone probe; consumers normally invoke the configured catalogue."""
    del argv
    return build({}).run()


def x_main__mutmut_1(argv: list[str] | None = None) -> int:
    """Standalone probe; consumers normally invoke the configured catalogue."""
    del argv
    return build(None).run()

mutants_x_main__mutmut['_mutmut_orig'] = x_main__mutmut_orig # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_1'] = x_main__mutmut_1 # type: ignore # mutmut generated


if __name__ == "__main__":
    sys.exit(main())
